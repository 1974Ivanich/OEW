# Дорожная карта развития OEW Motor FOC

## 🔴 Приоритет 1 — ближайшие задачи

### 1. OEW-измерение через оба инвертора
**Цель:** Измерить индуктивность рассеяния Lσ методом OEW (One-Energized-Winding) — одна фаза питается от Inv1, другая фаза замкнута через Inv2.

**Алгоритм:**
```
Inv1: фаза A (+) через HIN_U1
Inv2: фаза B (–) через LIN_U2 (или другая комбинация)
Измерение: di/dt в цепи A→B через оба инвертора
```

**Что даёт:** Lσ без влияния взаимоиндукции с ротором, более точная модель для FOC.

**Файлы:** `autotune.c`, `pwm.c` (управление TIM1+TIM8 одновременно)

### 2. Тест Rr (5 Гц синусоида)
**Цель:** Измерить активное сопротивление ротора Rr методом низкочастотной синусоидальной инжекции.

**Алгоритм:**
```
Подать синусоидальный ток 5 Гц на обмотки (Id=const, Iq=sin)
Измерить фазовый сдвиг между V и I
Rr = Re(Z) при ω→0
```

**Ограничения:** требует работающего FOC (контур тока Id/Iq) для генерации синусоиды. Если FOC не готов — реализовать через прямую PWM-модуляцию (V/f с синусоидальным заданием).

**Файлы:** `autotune.c`, `foc.c`

---

## 🟡 Приоритет 2 — среднесрочные задачи

### 3. Тест Lm/Lr (V/f холостой ход)
**Цель:** Измерить взаимную индуктивность Lm и полную индуктивность ротора Lr.

**Алгоритм:**
```
Разогнать двигатель до ~10% номинальной скорости (V/f)
Отключить PWM
Измерить затухание тока — постоянная времени Tr = Lr/Rr
```

**Что даёт:** Lm для модели FOC, Tr для наблюдателя потока.

**Требует:** V/f ramp-up (есть в `vf_start.c`), измерение BEMF через ADC (IN канал).

### 4. Запись осциллограммы (scope)
**Цель:** Захват и отображение переходных процессов тока/напряжения при импульсном тесте.

**Реализация:**
- Буфер на 512-1024 сэмплов ADC на STM32 (DMA или polling)
- Отправка по UART после завершения теста
- GUI: отображение графика (tk.Canvas)
- Формат: `@SCOPE:DT=10us:CH=IN:DATA=123,145,167,...`

**Файлы:** `scope.c`/`scope.h` (новый модуль), `nucleo_debug_tool.py` (график)

---

## 🟢 Приоритет 3 — перспективные задачи

### 5. Расчёт ПИ-регулятора
**Цель:** Автоматический расчёт коэффициентов Kp/Ki для PI-регуляторов тока и скорости на основе измеренных Rs, Ls, Rr, Lm.

**Метод:**
```
Kp_i = Ls / (3 * Ts)    (Ts = период ШИМ)
Ki_i = Rs / (3 * Ts)
Kp_speed = J / (3 * T_speed)
```

**Форматы:**
- CLI: `pi=task,value` или `pi=calc`
- Вывод: `@PI:ID:Kp=0.125:Ki=0.005` или `@PI:IQ:Kp=...:Ki=...`

**Файлы:** `autotune.c`, `main.c`, `nucleo_debug_tool.py`

### 6. Зависимость Lσ от положения ротора
**Цель:** Измерить, как меняется Lσ в зависимости от угла поворота ротора (saliency в PMSM).

**Алгоритм:**
1. Выровнять ротор в известное положение (DC импульс)
2. Измерить Lσ в этом положении
3. Повернуть ротор на N шагов (через FOC open-loop или механически)
4. Построить зависимость Lσ(θ)

**Что даёт:** Данные для high-frequency injection (HFI) sensorless control, Ld/Lq.

---

## Сводная таблица

| # | Задача | Приоритет | Зависимости | Файлы | Примерная сложность |
|---|--------|-----------|-------------|-------|--------------------|
| 1 | OEW через оба инвертора | 🔴 1 | PWM TIM1+TIM8 | autotune.c, pwm.c | средняя |
| 2 | Rr (5 Гц синусоида) | 🔴 1 | FOC или V/f | autotune.c, foc.c | средняя |
| 3 | Lm/Lr (холостой ход) | 🟡 2 | Vf_start, BEMF ADC | autotune.c, vf_start.c | высокая |
| 4 | Осциллограмма (scope) | 🟡 2 | ADC DMA | scope.c/h, GUI | низкая |
| 5 | PI-расчёт | 🟢 3 | Rs, Ls, Rr, Lm | autotune.c, main.c | низкая |
| 6 | Lσ(θ) | 🟢 3 | FOC alignment | autotune.c | средняя |
