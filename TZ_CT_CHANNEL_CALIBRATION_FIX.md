# ТЗ: диагностика и фикс CT-канала (PA6/ADC2_IN3) и калибровки токовых оффсетов

**Источник:** стендовая сессия 2026-08-23 (ПК-1, `ai-bench/bench-test`), no-HV.
**Характер:** диагностика, затем точечный фикс диагностики калибровки (замаскированный
FAIL) — safety-модуль `adc.c` **только по явному ТЗ** (AGENTS.md §7). Запуск
энергизированных тестов СТРОГО ЗАПРЕЩЁН до устранения причины.
**Затрагиваемые файлы (ОЖИДАЕМЫЕ):** `src/adc.c`, `src/adc.h`, `src/cli.c`
(печать результата калибровки), `main.c` (порядок вызова/допуск), тесты.
Фактический объём — по результатам диагностики (см. §3).

---

## 1. Проблема (симптомы на стенде)

Прошивка `origin/main` (95d132a), плата NUCLEO-G474RE + STEVAL-IPM20B×2,
DC-link развязан (VBUS≈0). Команды:

```
@ADC:I1=2040:I2=2068:Ires=0:VBUS=595        (a=200 — raw, сырые коды)
@ADC:CAL:offset_i1=0:offset_i2=0:offset_ires=0   (c — «успех»)
@FOC:I1=26081:I2=26439:Ires=0:VBUS=59935:...:FAULT=1:FAULT_R=18
@FAULT:CLEAR:STATUS=4                        (f — сброс отклонён)
1 → FAULT! send 'f' to clear                (FOC старт заблокирован)
```

Ключевые факты:

1. **VBUS читается корректно:** raw 595 → 59935 мВ ≈ 60 В при 0.48 В на PC4
   (делитель 1:125, `calc_vbus_mv`).
2. **CT-канал (PA6 / ADC2_IN3) даёт raw=0** — нижнее насыщение
   (`ADC_RAW_SAT_LOW=1`), т.е. сигнал физически не доходит / не читается.
3. **Калибровка `c` реально проваливается, но CLI показывает «успех»:**
   `ADC_CalibrateOffsets()` (adc.c:426–465) отбрасывает сэмплы с `rct<=1`;
   `valid < ADC_OFFSET_SAMPLES/2` → возврат `-1`, **но offsets остаются 0**
   (static init, при сбое не обнуляются/не помечаются). Команда `c` (cli.c)
   **игнорирует return code** и печатает `@ADC:CAL:offset_*=0` — FAIL
   замаскирован под успех.
4. Следствие: `idc1_ma = (2040−0)*3300*1e6/(4095*63000) ≈ 26.05 A`,
   `idc2_ma ≈ 26.4 A` — фантомные ~26 А при нулевом токе. Оффсеты не
   калиброваны (должны быть ≈2040).
5. **Fail-closed работает корректно:** `PROTECT_RequestClear()` (protect.c:185-191)
   видит `|I1|,|I2| > PROTECT_I_MAX_MA/2` → `STATUS=4 (VALUES_UNSAFE)`;
   FOC не стартует; PWM off (`CCER=0`, `MOE=0`). Это НЕ баг защиты — это
   правильная блокировка недоверенных измерений.

Диагноз должен подтвердить/опровергнуть: **почему PA6/CT raw=0** (обрыв,
solder bridge SB на MB1367, неподключённый CT, конфликт пина, тактирование
GPIO/ADC, порядок init) — и затем закрыть фиксом.

---

## 2. Требования (этап A — диагностика, этап B — фикс)

### Этап A. Диагностика (read-only, без изменения safety-кода)

1. **Изолировать физику канала:** `dumpa` (есть) + прозвонка мультиметром
   CN → вывод MCU PA6 (LQFP-64) с учётом solder bridge SBxx (UM2505/MB1367).
   Проверить напряжение на PA6 относительно GND при отключённом CT
   и при замкнутом CT (ожидание: около 1.65 В при нуле тока, как PA0/PA1).
2. **Проверить инициализацию:** порядок `PWM_BoardPins_Init()` → `ADC_Init()` →
   `ADC_InjectedInit()` в main.c; `GPIOA->MODER`/`GPIOA->PUPDR` для PA6
   (должен быть analog = 11, подтяжки off); `RCC->AHB2ENR` (GPIOA, ADC12EN).
   Всё это подтвердить readout'ом через существующие debug-команды или
   временный диагностический принт (обрамлять `#ifdef`, убрать до публикации).
3. **Проверить `adc_regular_read` по каналу 3:** таймауты, EOC, `DR`.
   При `raw=0` записать `ISR`/`DR` для CT-канала — отличить «не читается АЦП»
   от «читается 0».
4. **Проверить схему CT-пути STEVAL-IPM20B** (ШИМ-шунт/CT, коэффициент
   `ADC_CT_UV_PER_A=100000`): корректность подтяжки, полярность, наличие
   сигнала без тока.
5. По результатам оформить раздел «Корневая причина» с доказательствами
   (readout, прозвонка, схематичный путь сигнала). Если причина — solder
   bridge/аппаратура: инструкция по пайке/подключению в отчёте ТЗ.

### Этап B. Фикс (после подтверждения причины)

Обязательные пункты (независимо от найденной причины):

1. **CLI-команда `c` должна честно сообщать об ошибке.** Сейчас return code
   `ADC_CalibrateOffsets()` игнорируется (cli.c). Требование: при `rc != 0`
   печатать `@ADC:CAL:FAIL:reason=...` и НЕ показывать `offset_*=0` как успех;
   при `rc==0` — как сейчас (`@ADC:CAL:offset_*`). Это не меняет safety-
   логику, только диагностируемость.
2. **Фолбэк калибровки:** при недоступном CT-канале (raw стабильно ≈0/4095)
   калибровать I1/I2/VBUS, CT пометить `offsets_valid=0`-отдельно? — решить
   по корневой причине, НЕ ослабляя `adc_frame_status` (SATURATED остаётся
   для raw=0). Если CT физически отсутствует/неключевой для пуска — обсудить
   с приёмщиком политику (default-deny: FOC без CT разрешать?).
3. **Не гасить диагностику:** `@ADC:STATUS`, `dumpa`, `a?` остаются;
   добавить `calibration_fail_count` в доступную телеметрию, если нет.
4. **Тесты:** закрыть регрессию в `tests/` (hosted) на: (а) `c` при
   `CT=0` → FAIL не выглядит успехом; (б) при валидных сэмплах →
   `offset_*≈2040`; (в) `adc_frame_status` при `raw_ct=0` по-прежнему
   `ADC_FRAME_ADC_SATURATED` (не менять safety).
5. **Порядок инициализации** — если в диагностике найден дефект init
   (например, PA6 не переведён в analog или нет тактирования) — исправить
   именно init этого канала, не трогая остальные пины.

После фикса на стенде (без HV):

```
c        → @ADC:CAL:offset_i1≈2040:offset_i2≈2040:offset_ires≈2040
a        → @ADC:I1≈2040:I2≈2040:Ires≈2040:VBUS≈595
@FOC     → I1≈0:I2≈0:Ires≈0
f        → @FAULT:CLEAR:STATUS=0 (если latch снимается)
```

---

## 3. Критерии приёмки

- [ ] `make` (production, default-deny) — PASS;
- [ ] `make test` — ALL PASS (hosted+QEMU+pytest), включая новые тесты на
      калибровку;
- [ ] commissioning: `make EXTRA_CFLAGS="-DOEW_MAP_CAPTURE=1 -DOEW_MAP_L3=1
      -DPWM_OEW_BOARD_REVISION=7"` — PASS (если adc.c в generic-цепи);
- [ ] `git diff origin/main...HEAD --check` — чисто;
- [ ] diff ограничен файлами из §«Затрагиваемые» (по факту — adc.c/h, cli.c,
      tests, main.c только если init-дефект подтверждён);
- [ ] safety-файлы вне списка (protect.c, pwm.c, .ioc) — 0 строк;
- [ ] на стенде (по чек-листу приёмки, no-HV): `c` честно FAIL при CT=0
      (или PASS с правильными offsets после ремонта), `a` показывает
      Ires≈2040, `f` даёт валидный статус, FOC по-прежнему заблокирован
      до полной admission (MAP/окна) — fail-closed НЕ ослаблен;
- [ ] CI ветки — зелёный.

## 4. Запреты

- **Не** ослаблять fail-closed: `PROTECT_*`, `PWM_*`, `adc_frame_status` —
  0 строк, кроме явно согласованного с приёмщиком решения по CT-политике.
- **Не** запускать energised-тесты (FOC/V/f/ch*/mapcap) на стенде до
  полной приёмки этого ТЗ и разрешения приёмщика.
- **Не** менять `.ioc`, распиновку, `OEW_MAP_CAPTURE`/`OEW_MAP_L3`.
- **Не** «чинить» попутно другие модули (AGENTS_WORKFLOW §4.7).
- Временные диагностические принты — только `#ifdef`, удалить до публикации.

## 5. Процесс

Ветка `ai-bench/ct-fix` от свежего `origin/main` (95d132a), запись в
`docs/AGENTS_STATUS.md`, публикация + `git ls-remote` подтверждение SHA,
ожидание приёмки (main не трогать). Результат диагностики — раздел
«Корневая причина» в этом ТЗ (дополнить) или отдельный `TZ_CT_DIAG_RESULT.md`.

## 6. Статус исполнения (2026-08-23)

### Этап A — диагностика завершена

- Программная часть АЦП корректна: `JSQR=0x00A18482` (каналы 2/3/5),
  `GPIOA pin6` analog-режим, `ADEN=1`, VBUS-канал читается (`DR≈598`).
- **Найдена причина `raw_ct=0` — топология, а не обрыв**: CT подключён
  напрямую к PA6 БЕЗ ОУ (в отличие от I1/PA0, I2/PA1 через ОУ со смещением
  ~1.65 В). При нулевом токе на PA6 реально 0 В → raw=0.
- `protect.c` не использует `raw_ct`/`ict_ma` — CT/Ires диагностический
  (zero-sequence), в защите не участвует.

### Этап B — фикс выполнен

- `src/adc.c` (коммит `e21d72b`): добавлена `adc_ct_sample_is_usable()` —
  для Ires допустим нулевой уровень (raw≈0..1), верхняя рейка (≥4094)
  остаётся признаком насыщения. Применена в `adc_frame_status()` и
  `ADC_CalibrateOffsets()`.
- CLI `c` — честный FAIL при `rc≠0` (коммит `2c0cc09`).
- Тест `tests/adc_frame_host_test.c` обновлён: ires=0 → калибровка OK +
  VALID (при открытой admission); верхняя рейка Ires → SATURATED;
  нижняя рейка шунтов → fail-closed.

### Подтверждение на стенде (no-HV)

```
c        → offset_i1=2040:offset_i2=2068:offset_ires=0   (SUCCESS)
1        → @FOC:START:FAIL:rc=-2 (map_unverified)         (fail-closed сохранён)
p?       → @PWM:CR1=224:CCER=0 (PWM off)
@FOC     → I1≈0..-63мА, I2≈0, Ires=0, FAULT=0
```

Fail-closed для I1/I2/VBUS и admission (`control_admitted`) **не изменены** —
energise остаётся заблокирован до квалификации токовой карты (L3/mapcap).

## 7. Безопасность стенда (напоминание)

HV/DC-link не подавать, обе шины < 1 В (подтверждено VBUS raw≈595 → 0.48 В).
После приёмки ветки и квалификации L3 — energise по `BENCH_FIRST_SESSION.md` §5.
