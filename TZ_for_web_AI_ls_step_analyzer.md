# ТЗ для web AI: анализатор лога LS-STANDSTILL-STEP (`@AT:LS:*`)

Проект: STM32G474 (CMSIS-only), OEW FOC для асинхронного двигателя (2×STEVAL-IPM20B).
Файл ТЗ: `TZ_for_web_AI_ls_step_analyzer.md`. Задача — **только offline-код и тесты**:
железо, прошивка, safety-модули не затрагиваются (см. «ЗАПРЕТЫ»).

## 1. Проблема

На стенде выполняется диагностический тест `ls` (прошивка `Autotune_LsStep()`, `src/autotune.c`):
на неподвижном роторе подаются три уровня дифференциального напряжения `d = 5 / 10 / 15 %`,
на каждом уровне пишется временной ряд кадров ADC и печатается `@AT:LS:RESULT`.
Разбор делается вручную по логу UART: медленно и невоспроизводимо. Нужен детерминированный
инструмент, который считает одинаково, печатает явные причины отбраковки и **не выдаёт
«число Ls» без оценки его состоятельности**.

## 2. Цель

Написать `tools/ls_step_analyze.py` и `tests/test_ls_step_analyze.py`, которые:

1. читают сырой лог UART с `@AT:LS:*` и восстанавливают ряд кадров по каждому уровню `d`;
2. независимо пересчитывают `dt`, `ΔI`, `dI/dt` из сырых кадров (число `Lstep_uH` из
   `@AT:LS:RESULT` — только для сверки, не источник истины);
3. считают ДВЕ оценки индуктивности (см. §4.3) и коэффициент обусловленности;
4. выполняют ПЯТЬ независимых проверок (§4.4) и печатают вердикт с причинами.

Инструмент только ЧИТАЕТ лог и пишет артефакты. Ничего не применяет к мотору и не
рекомендует значение для `mp=`.

## 3. Вход: грамматика телеметрии (дословно из прошивки)

Строки заканчиваются `\r\n`; между ними допустимы любые посторонние строки (`@SYS`,
`@MC:REC`, приглашение `> `, мусор) — парсер обязан их игнорировать.

```
@AT:LS:START
@AT:LS:LEVEL:d=<d>:U_eff_mv=<мВ>:ccr_hi=<u16>:ccr_lo=<u16>:arr=<u16>
@AT:LS:FRAME:d=<d>:n=<n>:ph=<0|1>:t=<такты CYCCNT>:I1=<мА>:I2=<мА>:Idiff=<мА>:Vbus=<мВ>:CCR1=<u16>:CCR8=<u16>
@AT:LS:RESULT:d=<d>:Lstep_uH=<мкГн>:n_valid=<n>:Rs_mOhm=<мОм>:SEMANTICS=Lstep_not_confirmed_Ls
@AT:LS:DONE
```

Аварийные строки (завершают прогон):

```
@AT:LS:ERROR:OFFSETS_NOT_VALID
@AT:LS:ERROR:VBUS_LOW:<мВ>
@AT:LS:ERROR:ADC_ARM_FAIL
@AT:LS:ERROR:PWM_ARM_FAIL:<знаковый код>
@AT:LS:ERROR:OVERCURRENT I1=<мА> I2=<мА>
@AT:LS:ABORTED
@AT:LS:WARN:RESET_FAIL:D=<d>
```

Семантика полей (важно, не перепутать):

| Поле | Смысл |
|---|---|
| `d` | уровень задания: целое 5 / 10 / 15 (%) |
| `n` | номер кадра в уровне, 0…19; `n = 0..3` — до фронта, `n = 4..19` — на ступени |
| `ph` | фаза кадра: **0 = pre (нуль-вектор), 1 = step** (НЕ номер фазы двигателя) |
| `t` | `DWT->CYCCNT` — **такты CPU** (ядро 170 МГц): `dt_us = dt_cycles / (CPU_HZ/1e6)` |
| `I1`, `I2` | токи инверторов, мА |
| `Idiff` | `(I1 − I2)/2`, мА (Inv1 «положительно», Inv2 «отрицательно») |
| `Vbus` | напряжение звена, мВ (инжектированное, синхронное с ШИМ) |
| `CCR1`, `CCR8` | снимок `TIM1->CCR1` / `TIM8->CCR1` в кадре |
| `U_eff_mv` | `Vbus(первый кадр ступени) × 2 × d / 100`, мВ — дифференциальное напряжение на обмотке |
| `arr` | `PWM_GetARR()`: `TIM1/TIM8 ARR` при **центрированном** счёте (CMS=3, такт счётчика 10 МГц) |

Модель периода ШИМ (из `PWM_Init()`: `psc_plus1 = tck/10 МГц`, `arr_plus1 = (10 МГц + 5000)/(5000·2) = 1000`):

```
pwm_period_us = 2 · (arr + 1) / tclk_mhz        # tclk_mhz по умолчанию 10 → arr=999 ⇒ 200.0 мкс
```

Один кадр — ровно один период ШИМ, поэтому медиана измеренных `dt` обязана совпасть с
`pwm_period_us` (см. проверку `PWM_PERIOD`). Это НЕ подстановка: несовпадение — это
находка о модели таймера, её надо напечатать, а не «подогнать».

Полный уровень — 20 кадров (4 pre + 16 step); успешный прогон содержит три уровня.

Пример (реалистичные значения: `arr=999`, период 200 мкс = 34 000 тактов CPU, `Rs=13 Ω`):

```
@AT:LS:LEVEL:d=5:U_eff_mv=3010:ccr_hi=549:ccr_lo=451:arr=999
@AT:LS:FRAME:d=5:n=0:ph=0:t=1048576:I1=11:I2=-9:Idiff=10:Vbus=30100:CCR1=500:CCR8=500
@AT:LS:FRAME:d=5:n=1:ph=0:t=1082576:I1=9:I2=-7:Idiff=8:Vbus=30100:CCR1=500:CCR8=500
@AT:LS:FRAME:d=5:n=4:ph=1:t=1150576:I1=232:I2=-228:Idiff=230:Vbus=30100:CCR1=549:CCR8=451
@AT:LS:FRAME:d=5:n=5:ph=1:t=1184576:I1=240:I2=-236:Idiff=238:Vbus=30100:CCR1=549:CCR8=451
```

## 4. Что обязан посчитать инструмент

### 4.1. Пары кадров

Пары берутся только среди кадров ступени (`ph = 1`) одного уровня, по порядку `n`;
для пары `(a,b)` = кадры `n` и `n+1`:

```
dt_us   = (b.t − a.t) / (CPU_HZ / 1e6)
dI_ma   = b.Idiff − a.Idiff
i_mid   = (a.Idiff + b.Idiff) / 2
dIdt    = dI_ma / dt_us                                  # мА/мкс
Rs_mOhm = из строки RESULT своего уровня; если её нет — --rs-mohm (по умолчанию 13000)
L_lin   = (U_eff_mv − i_mid·Rs_mOhm/1000) · dt_us / dI_ma    # мВ·мкс/мА = мкГн (двухточечная)
```

Правила допуска пары (каждая отбраковка печатается с причиной и номером кадра):

- `dt_us` вне `[pwm_period_us·(1−dt_tol); 2000]` → `REJECT:DT_OUT_OF_ENVELOPE`;
- `|dI_ma| < --min-di-ma` (по умолчанию 50 мА) → `REJECT:DI_TOO_SMALL`;
- `dI_ma ≤ 0` → `REJECT:DI_NONPOSITIVE`;
- нет строки LEVEL для уровня (`U_eff_mv` неизвестен) → `REJECT:NO_LEVEL_LINE`;
- `L_lin ≤ 0` → `REJECT:L_NONPOSITIVE`.

Два режима подсчёта по одним и тем же допущенным парам (печатать оба):

- **`firmware_mode`** — как в прошивке: пары с `dI_ma ≥ 20 мА`, медиана `L_lin`
  (сверка с `Lstep_uH` из `RESULT`; расхождение > 1 % → `WARN:FIRMWARE_MISMATCH`);
- **`all_pairs`** — все допущенные пары: медиана, min, max, размах %.

### 4.2. Уровень: ряд ступени для фита

Для каждого уровня взять кадры `ph=1`, принять `t0` = время первого кадра ступени,
`I_ss` — оценка установившегося тока (`= U_eff_mv / (Rs_mOhm/1000)` в мА; это верхняя
граница, если измеренный ряд её не достигает — так и напечатать).

### 4.3. Две оценки индуктивности (обе обязательны!)

1. **Линейная (двухточечная)** — `L_lin` по §4.1, медиана; это то, что считает прошивка.
   Она корректна только если `τ << dt` (быстрая ступень).
2. **Экспоненциальный фит** — `Idiff(t) = I_ss · (1 − exp(−(t−t0)/τ))` по кадрам ступени
   (МНК по τ, сеточный поиск или замкнутая форма; без внешних библиотек).
   Тогда `L_fit_uH = τ_us · Rs_mOhm / 1000` (мкс·Ом = мкГн).
   Печатать `tau_us`, `n_points`, остаток фита (среднеквадратичное отклонение в мА).
3. **Обусловленность**: `cond = tau_us / med_dt_us`. Печатать всегда.
   Смещение двухточечной оценки равно `θ/(1 − e^(−θ))`, где `θ = 1/cond = dt/τ`:
   `cond = 3` → +18 %, `cond = 5` → +11 %, `cond = 10` → +5 %, `cond = 30` → +1.7 %.
   Отсюда пороги:
   - `cond ≥ 10` (смещение < 6 %) → линейная оценка пригодна; требовать совпадения
     `L_lin` и `L_fit` в пределах 10 %, расхождение больше — `FAIL:ESTIMATOR_MISMATCH`;
   - `3 ≤ cond < 10` → `L_lin` систематически выше на 6…18 %: авторитетна `L_fit`,
     расхождение печатать как ОЖИДАЕМОЕ, а не как ошибку;
   - `0.5 ≤ cond < 3` → авторитетна `L_fit`, `L_lin` — только сверка с прошивкой;
   - `cond < 0.5` → ряд почти не содержит переходного процесса: вердикт
     `CONDITIONING = FAIL:INSUFFICIENT_TRANSIENT` (измерение не даёт L, см. §4.4).

   Обоснование (обязательно в докстринге модуля): при `Rs = 13 Ω` и `L = 800 мкГн`
   `τ = 61 мкс`, а период ШИМ 200 мкс, т.е. ток достигает ~96 % установившегося значения
   внутри ОДНОГО периода; двухточечная формула `U·dt/ΔI` даёт при этом завышение
   в ~3 раза. Стенд `ls` спроектирован с шагом 200 мкс, поэтому математика должна
   это учитывать, а не молча выдавать смещённое число.

### 4.4. Пять независимых проверок (по каждой — вердикт и причина)

1. **`PWM_PERIOD`**: медиана измеренных `dt` против `pwm_period_us` из модели §3.
   Расхождение > `--period-tol` (по умолчанию 0.05) → `FAIL:PERIOD_MODEL_MISMATCH`
   (значит, модель таймера неверна — печатать оба числа, ничего не «исправлять»).
2. **`SIGN`**: знак `dI_ma` во всех допущенных парах ступени одинаков (ожидается
   положительный). Смешанные знаки → `FAIL` с номерами кадров обратного знака.
   Кадры `ph=0` не участвуют.
3. **`SHAPE`**: плато нуль-вектора (кадры `ph=0`) — `max|Idiff| ≤ --pre-plateau-ma`
   (по умолчанию 100 мА), без тренда (разность первого и последнего кадра плато
   ≤ `--pre-plateau-ma`); фронт — первый кадр ступени, где `|Idiff| ≥ 10 × max|Idiff(pre)|`.
   Нарушение → `FAIL` с номером кадра («нуль-вектор не обнуляет» / «ступень не разрешается»).
4. **`CONVERGENCE`**: медианы `all_pairs` по уровням 5/10/15 %; `max|med_i − med_j| / среднее
   ≤ --conv-tol` (по умолчанию 0.25) → `PASS`, иначе `FAIL` с таблицей по уровням.
   Дополнительно: те же уровни сравниваются по `L_fit` (печатать обе таблицы).
5. **`PLAUSIBILITY`** + **`CONDITIONING`**:
   - `L_fit` (а при `cond ≥ 10` — и медиана `L_lin`) внутри окна FOC `500 … 500000 мкГн`
     → `PLAUSIBILITY = PASS`, иначе `FAIL:OUT_OF_RANGE` (печатать окно рядом со значением);
   - `CONDITIONING = PASS` только при `cond ≥ 0.5`; иначе `FAIL:INSUFFICIENT_TRANSIENT`.
   Обе проверки НЕ являются утверждением о моторе — только о состоятельности измерения.
   Никогда не подставлять и не предлагать значение для `mp=`.

### 4.5. Целостность прогона

- нет `@AT:LS:START` → код возврата 2, `VERDICT=UNREADABLE`, ничего не считать;
- есть `START`, нет `@AT:LS:DONE` → `INCOMPLETE`;
- любая аварийная строка из §3 → `INCOMPLETE` с дословным текстом строки
  (например `PWM_ARM_FAIL:-5`);
- уровень без единой допущенной пары → `INCOMPLETE` (не «ноль»);
- `WARN:RESET_FAIL:D=<d>` → предупреждение уровня `d`, учитывается в `SHAPE`;
- считаются все три уровня `d = 5/10/15`, иначе `INCOMPLETE`.

## 5. Контракт CLI

```
py -3 tools/ls_step_analyze.py <log.txt> [--json out.json]
        [--cpu-hz 170000000] [--tclk-mhz 10] [--rs-mohm 13000]
        [--min-di-ma 50] [--dt-tol 0.15] [--period-tol 0.05]
        [--pre-plateau-ma 100] [--conv-tol 0.25]
```

- Коды возврата: `0` — `USABLE`, `1` — `UNUSABLE`, `2` — `VERDICT=UNREADABLE`.
- `USABLE` только если все проверки §4.4 `PASS`, все три уровня посчитаны, аварийных строк
  нет, и у каждого уровня есть обе оценки (`L_lin`-медиана и `L_fit`).
- JSON пишется ДО печати в stdout; stdout — UTF-8 (`sys.stdout.reconfigure(encoding="utf-8")`);
  запись файла — `encoding="utf-8"`, `newline="\n"`. Лог читать `errors="replace"`,
  поддерживать CRLF и LF.

Схема JSON (ключи обязательны):

```json
{
  "tool": "ls_step_analyze",
  "schema": "tz-ls-step-analyze-1",
  "log": {"path": "...", "sha256": "...", "lines": 1234, "crlf": true},
  "env": {"cpu_hz": 170000000, "tclk_mhz": 10, "rs_mohm": 13000, "min_di_ma": 50,
          "dt_tol": 0.15, "period_tol": 0.05, "pre_plateau_ma": 100, "conv_tol": 0.25},
  "levels": [
    {"d": 5, "u_eff_mv": 3010, "arr": 999, "pwm_period_us": 200.0, "n_frames": 20,
     "firmware": {"Lstep_uH": 2680, "n_valid": 15, "median_lin_uH": 2680, "mismatch_pct": 0.3},
     "all_pairs": {"median_lin_uH": 2680, "min_uH": 2540, "max_uH": 2820,
                   "spread_pct": 10.4, "n_accepted": 14, "n_rejected": 1},
     "fit": {"L_fit_uH": 812, "tau_us": 62.5, "i_ss_ma": 232, "n_points": 16,
             "residual_rms_ma": 4.1},
     "cond": 0.3125,
     "pairs": [{"n": 4, "dt_us": 200.0, "dI_ma": 228, "i_mid_ma": 119, "dIdt_ma_us": 1.14,
                "L_uH": 2660, "accepted": true, "reason": null}],
     "checks": {"SIGN": "PASS", "SHAPE": "PASS"}}
  ],
  "checks": {"PWM_PERIOD": "PASS", "SIGN": "PASS", "SHAPE": "PASS",
             "CONVERGENCE": "PASS", "PLAUSIBILITY": "PASS", "CONDITIONING": "FAIL"},
  "data_integrity": {"start": true, "done": true, "fault_lines": [], "levels_seen": [5, 10, 15]},
  "verdict": "USABLE",
  "reasons": ["WARN:CONDITIONING_FAIL:cond=0.31 — линейная оценка смещена вверх"]
}
```

## 6. Проверки инструмента (pytest, минимум 25, все PASS)

Тесты генерируют синтетические логи строкой внутри теста (прошивка и железо не нужны).
Генератор задаёт `L_true`, `Rs`, `U_eff` и строит ряд по ФИЗИКЕ первого порядка:

```
I(t) = I_ss · (1 − exp(−(t−t0)/τ)),  τ = L_true_uH / (Rs_mOhm/1000) [мкс],  I_ss = U_eff_mv/(Rs_mOhm/1000)
```

Обязательные тесты:

1. `L_true = 800 мкГн`, `Rs = 13000`, три уровня → `L_fit` в пределах 5 % от 800;
2. тот же лог → `L_lin` медиана ЗАВЕДОМО выше 800 (смещение), и `cond ≈ 0.31`;
3. `L_true = 40000 мкГн` (τ = 3077 мкс, `cond ≈ 15`) → `L_lin` и `L_fit` совпадают
   в пределах 10 % (проверка согласия оценок на хорошо обусловленном ряде);
4. `L_true = 200 мкГн` (τ = 15 мкс, `cond ≈ 0.08`) → `CONDITIONING = FAIL`, код 1;
5. `L_true = 8000 мкГн` (τ = 615 мкс, `cond ≈ 3`) → `L_fit` в пределах 5 %, `L_lin` выше
   на 6…18 % (систематическое смещение), вердикт остаётся `USABLE` при `cond ≥ 0.5`;
6. синтетический = линейный ряд (`I = k·t`, что соответствует очень большой L) →
   `L_lin` совпадает с заданной формулой в пределах 1 % (проверка арифметики §4.1);
7. `arr = 999`, `dt = 200 мкс` → `PWM_PERIOD = PASS`; `arr = 999`, `dt = 400 мкс` →
   `PWM_PERIOD = FAIL:PERIOD_MODEL_MISMATCH`;
8. `--tclk-mhz 20` меняет модель периода и результат проверки 6 (с `dt=100 мкс` → PASS);
9. уровень только `d=5` → `INCOMPLETE`, код 1;
10. смешанные знаки `ΔI` → `SIGN = FAIL`, код 1;
11. pre-кадры с `|Idiff| = 400 мА` → `SHAPE = FAIL`, код 1;
12. один кадр с аномальным `dt` (имитация halt ядра отладчиком) → пара
    `REJECT:DT_OUT_OF_ENVELOPE`, остальные приняты, причина в отчёте;
13. все пары уровня с малым `ΔI` → уровень `INCOMPLETE`, код 1;
14. все пары `dI_ma ≤ 0` → `SIGN = FAIL`;
15. медианы уровней 800 и 1600 мкГн → `CONVERGENCE = FAIL`, код 1;
16. медиана 200 мкГн → `PLAUSIBILITY = FAIL:OUT_OF_RANGE`;
17. медиана 900000 мкГн → `PLAUSIBILITY = FAIL:OUT_OF_RANGE`;
18. лог обрывается без `DONE` → `INCOMPLETE`, код 1;
19. лог без `START` → код 2, `VERDICT=UNREADABLE`;
20. `@AT:LS:ERROR:PWM_ARM_FAIL:-5` → `INCOMPLETE`, строка дословно в `fault_lines`;
21. `@AT:LS:ERROR:OVERCURRENT I1=7000 I2=-200` → `INCOMPLETE`, строка сохранена дословно;
22. `firmware_mode` сверяется с `Lstep_uH`: расхождение > 1 % → `WARN:FIRMWARE_MISMATCH`
    в `reasons`, вердикт при этом НЕ меняется на FAIL;
23. посторонние строки, CRLF и LF с мусором → результат идентичен, парсер не падает;
24. `--json out.json` пишет файл до печати: валидный JSON, ключи §5, LF; при недоступном
    каталоге — код 1 и сообщение, без стектрейса;
25. `--rs-mohm 0` вырождает формулу в `U_eff·dt/ΔI` (проверить на известном примере);
26. остаток фита (`residual_rms_ma`) на «идеальном» синтетическом ряде < 1 мА,
    а на ряде с добавленным шумом ±20 мА — больше.

Тесты доказывают ТОЛЬКО инструмент (парсинг, арифметику, правила отбраковки, фит).
Они НЕ доказывают физику мотора и НЕ подтверждают, что `ls` на стенде измерит Ls.
Эта формулировка обязательна в докстринге теста.

## 7. Формат вывода (stdout)

```
=== LS-STANDSTILL-STEP analyzer ===
лог: <path> · sha256=<...> · строк=<N> · CRLF=да/нет
start=да done=нет аварийных строк=1 [@AT:LS:ERROR:PWM_ARM_FAIL:-5]

уровень d=5%  U_eff=3010 мВ  arr=999  период ШИМ=200.0 мкс  кадров=20
  firmware:  Lstep=2680 мкГн  n_valid=15   (медиана_lin=2680, расхождение 0.3%)
  all_pairs: медиана_lin=2680  min=2540  max=2820  размах=10.4%  принято=14  отклонено=1
  фит:       L_fit=812 мкГн  tau=62.5 мкс  I_ss=232 мА  точек=16  остаток=4.1 мА
  обусловленность: cond=0.31  → линейная оценка смещена ВВЕРХ, авторитетна L_fit
  отклонённые: n=9 dt=4300.0 мкс REJECT:DT_OUT_OF_ENVELOPE
  SIGN=PASS  SHAPE=PASS

...
PWM_PERIOD:   PASS  (модель 200.0 мкс, медиана измеренных dt 200.0 мкс)
CONVERGENCE:  PASS  (медианы L_lin 2680/2720/2660, отклонение 2.2%; L_fit 812/806/830)
PLAUSIBILITY: PASS  (окно 500…500000 мкГн, L_fit=812)
CONDITIONING: FAIL  (cond=0.31 < 0.5 — переходного процесса в одном периоде недостаточно)

ВЕРДИКТ: UNUSABLE
причины:
  - CONDITIONING: FAIL:INSUFFICIENT_TRANSIENT
  - CONDITIONING: линейная оценка смещена вверх в ~3 раза (см. §4.3)
```

Никаких рекомендаций по `mp=`/`Ls` в выводе быть не должно.

## 8. Сборка и прогон (сделать самому обязательно)

```bash
py -3 -m pytest tests/test_ls_step_analyze.py -q
py -3 tools/ls_step_analyze.py <лог> --json out.json; echo "exit=$?"
```

Приложить вывод обеих команд. Если pytest недоступен — сообщить, не выдумывать результат.

## 9. Запреты

- НЕ изменять: `src/**` (в т.ч. `autotune.c`, `foc.c`, `pwm.c`, `adc.c`, `protect.c`),
  `main.c`, `Makefile`, `*.ioc`, `tests/*.c`, `tests/mocks/**`, существующие `tools/*.py`.
- НЕ добавлять зависимости (только stdlib Python 3.11+: `math`, `json`, `argparse`, `re`,
  `statistics`, `hashlib`, `sys`).
- НЕ обращаться к сети, не требовать железа, не запускать `make`, не трогать UART.
- НЕ парсить лог «по похожести»: только строгий разбор по грамматике §3.
- НЕ подставлять `Ls` в `mp=`, не печатать «рекомендуемое Ls», не менять вердикт под ожидания.
- НЕ логировать содержимое лога целиком в JSON (только метрики и метаданные).
- НЕ «подгонять» модель периода под измеренный `dt`: расхождение — это находка (`FAIL`).

## 10. Ответ должен содержать

1. Полный код `tools/ls_step_analyze.py` и `tests/test_ls_step_analyze.py`.
2. Вывод `py -3 -m pytest tests/test_ls_step_analyze.py -q` (число тестов и результат).
3. Вывод прогона инструмента на своём синтетическом логе из теста 1 (видно обе оценки).
4. Явный список мест, где ТЗ оказалось неоднозначным, и как решено.
5. Если найден дефект в грамматике/семантике прошивки — сообщить, НЕ правя прошивку.

## 11. Критерии приёмки (проверяет ПК-2)

1. `py -3 -m pytest tests/test_ls_step_analyze.py -q` — зелёный, ≥ 25 тестов.
2. Ни один файл из §9 не изменён (проверка диффом).
3. На синтетике с известными `L_true` и `Rs`: `L_fit` в пределах 5 %; на медленном ряде
   (`cond ≥ 3`) `L_fit` и `L_lin` совпадают.
4. На ряде с `cond < 0.5` вердикт `UNUSABLE` с `CONDITIONING: FAIL` (а не «число без оговорок»).
5. На логе с аварийной строкой и на обрыве — `INCOMPLETE`, код 1, причина названа дословно.
6. Инструмент не предлагает значений для `mp=`; `WARN:FIRMWARE_MISMATCH` не меняет вердикт.
