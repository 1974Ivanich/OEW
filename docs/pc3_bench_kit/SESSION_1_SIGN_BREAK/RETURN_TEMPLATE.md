# Шаблон возврата: сессия «знак + break» (PC-3)

Образ (SHA256): `96e9747028a5bf6ff7bf87a4e825dbe930e43d4545b8dcaaa2f9a80538d11d64`
Плата / стенд:  Дата:  Оператор:

## Шаг 0 — безопасное состояние (до любых действий)

| Проверка | Ответ (вставить вывод) |
|---|---|
`p?` → `CR1/CCER/BDTR/CNT` | |
`dump` (hex BDTR TIM1) | |
`dump8` (hex BDTR TIM8) | |
`pdump` | |
`breakdiag` (если valid=1 — вывода + файл) | |
`breakdiag reset` выполнен | да / нет |
VBUS (БП или осциллограф) | В |

## Шаг 1 — знак энкодера БЕЗ питания

| Вращение | Δ`angle` (знак) | `speed` (знак) | `err` |
|---|---|---|---|
| по часовой (вид со стороны вала) | | | |
| против часовой | | | |
| покой (baseline) | | | |

Вывод `TOOLS/enc_sign.py` по захваченному логу `enc`: вставить целиком.
Сколько оборотов делали рукой: ___ (важно: если < 0.25 оборота — вывод о знаке не делать).

## Шаг 2 — решающий тест: перестановка двух моторных проводов

| Пункт | Ответ |
|---|---|
Какие провода поменяли (U↔V и т.п.) | |
Прогон до перестановки: знак `meas`/`eangle` | |
Прогон после перестановки: знак `meas`/`eangle` | |
Лог после перестановки (имя файла) | |
Вывод по таблице `START_HERE_PC3.md` (инвертирован энкодер / инвертирован порядок фаз) | |
Провода возвращены в исходное состояние | да / нет |

## Шаг 3 — силовой прогон с измерителями

| Пункт | Ответ |
|---|---|
`dump`/`dump8`/`pdump` ДО (файлы) | |
`breakdiag reset` перед прогоном | да / нет |
Прогон: `vf=`, `vflog=`, длительность | |
Событие: break / watchdog-2с / иное | |
`breakdiag` ПОСЛЕ (сырой файл) | |
Вывод `TOOLS/breakdiag_parse.py` по нему | |
`dump`/`dump8`/`p?` ПОСЛЕ | |
Осциллограф CH1−CH2: **V/дел** и **время/дел** (обязательно на скриншоте) | |
Форма дифференциала: 0 / −12 / +12 В при `du` 50/40/60 % — совпало? | |
Знак дифференциала совпал со знаком `(du−50 %)`? | |
SD/EM_STOP: был ли спад/импульс, его длительность, момент относительно роста тока | |
15 В рейл модуля во время прогона (min/max) | |
VBUS во время прогона (осциллограф/БП, min) | В |

## Итоговое состояние стенда

`p?` после сессии (CCER / MOE / FAULT): ___ · питание снято: да/нет · COM4 освобождён: да/нет

## Напоминание (что НЕ делать в этой сессии)

VF-2B/VF-3 не запускать · `c`/`ch`/`iv` не запускать · `vmag`/`fslip`/пороги защиты **не менять** ·
знак и break — это **измерения**, а не повод для правок в разгар сессии; правки — отдельным ТЗ
после того, как причина установлена.
