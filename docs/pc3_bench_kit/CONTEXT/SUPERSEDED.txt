# Что заменено и что нельзя запускать (обновлено 26.09.2026, комплект v2)

Актуальная и единственная инструкция на носителе — `START_HERE_PC3.md` этого комплекта (v2).
Комплект v1 (`pc3_bench_kit_20260926`) снят с носителя и лежит в
`_SUPERSEDED/pc3_bench_kit_20260926_v1/` (не удалён; его собственный `SHA256SUMS` внутри
остался действителен, содержимое v1 не изменялось). Манифест карантина — `_SUPERSEDED/SHA256SUMS`.

## Заменено этим комплектом

| Объект v1 | Статус | Куда перенесено содержимое |
|---|---|---|
| `pc3_vf2_sign_break_pkg_20260926/` | заменён протоколом v2 (исправлены инструменты, кодировка консоли, пути) | `SESSION_1_SIGN_BREAK/` |
| `vf_baseline_energize_20260924/` | заменён протоколом v2 | `SESSION_2_VF2_VF3/` + `IMAGE/` |
| `pc3_vf2_audit_20260926/` | заменён | `CONTEXT/` |

## Проверено фактически на носителе (26.09.2026, аудит v1)

В v1 было сказано, что перечисленные ниже объекты сняты в `_SUPERSEDED/`. Фактически папки
`_SUPERSEDED/` в v1 не существовало, а рекурсивный поиск по носителю нашёл только два объекта:

* `pc3_ch_audit_20260926/` — лежал в `oew_inbox/` (вне комплекта); **перенесён** в
  `_SUPERSEDED/pc3_ch_audit_20260926/`;
* сам комплект v1 — **перенесён** в `_SUPERSEDED/pc3_bench_kit_20260926_v1/`.

Остальные объекты из того списка на носителе **не найдены** (ни на верхнем уровне, ни в `oew_inbox/`):
`pc3_ch_uplink_20260924/`, `ch_log_tools_20260924/`, `pc3_vf2_sign_break_pkg_20260926/`,
`vf_baseline_energize_20260924/`, `pc3_vf2_audit_20260926/`, `PC3_TZREF01_PHASE0_20260924_README.md`.
Канонические копии — на ПК-2; на этом носителе их не было.

## Не запускать (устаревшие инструкции; цель недостижима на текущем образе)

* протокол `c -> ch -> iv`: `c`/`ch` недостижимы fail-closed (`@AT:ERROR:POWER_BLOCKED`,
  src/autotune.c:1404-1412);
* `ch_log_tools_20260924` — инструмент разбора ch-лога: ch-лога на этом образе не будет;
* `pc3_ch_audit_20260926` — аудит возврата по CH (в карантине, как история, не инструкция);
* `PC3_TZREF01_PHASE0_20260924_README.md` — TZ-REF-01 закрыт.

История кампаний в корне носителя (папки `ls_*`, `mapcap_*`, `phase_ref_*`, `tzref01_*`, `pc3_*`)
и папка `pc3_vf2_return_20260926/` (входные данные аудита VF-2A) — **данные и история, не
инструкции**: запускать по ним что-либо «по памяти» нельзя, сверяться только с этим комплектом.

Статус ACS712-трека: NOT QUALIFIED / HOLD (отложен до отдельного решения), пакеты лежат на
носителе как история. TZ-REF-01 — закрыт.

Правило замены (регламент): заменяющий пакет объявляется явно, устаревший снимается с носителя
в карантин. Содержимое устаревших пакетов перенесено внутрь комплекта; канонические копии
остаются на ПК-2 в `C:\campaign_raw\` (локальный путь ПК-2; в инструкциях комплекта абсолютных
путей носителя нет намеренно — буква диска на ПК-2 и ПК-3 разная).
