# TZ_FOC_POLE_PAIRS_3 — исправить default pp: 6 → 3 (мотор 6-полюсный = 3 пары)

## Цель
Исправить неверный default `FOC_DEFAULT_POLE_PAIRS = 6` в `src/foc.c` на **3**.
Мотор на стенде — 6-полюсный ACIM (шильдик «6 полюсов» = **3 пары полюсов**).
При pp=6 V/f и FOC считают поле в 2 раза выше реального: синхронная скорость
в 2 раза выше цели, vmag уходит в насыщение.

## Условия
- Железо: STM32G474RE + 2× STEVAL-IPM20B, DC-link 60 В, мотор 6-полюсный ACIM.
- База: `origin/main` (f4fcff5 — overshoot + start-reliability приняты).
- Стенд: пакет `vf_fix_pkg_vf_fixes_20260901` (f4fcff5, energize + monitor-only).

## Проблема (баг-репорт, стенд 01.09.2026)
Прогон пакета f4fcff5 (overshoot-фикс) на стенде:
- **pp=6 (default): 0/10 PASS** — max_meas 451–522 RPM при цели 300, max_fe 21 Гц,
  max_vmag 57. Мотор физически крутится быстрее поля: 500 RPM при fe=21 Гц =
  sync 210 @ pp=6 — в двигательном режиме невозможно (мотор бы обгонял поле
  только в генераторном/рекуперативном). Значит фактический pp ≠ 6.
- **pp=3 (CLI `pp=3`): 10/10 PASS** — max_meas 303–331 (≤360), max_fe 12–13,
  max_vmag 39–41. Синхронная при f_e=15 Гц и pp=3 = 300 RPM = цель. vmag ~40%
  вместо 95 → ложный FAULT_N STEVAL-2 (SD2) больше не возникает (0 провалов
  за 10 стартов) — вторичный эффект исправления.
- `pp=3` задан через CLI (RAM) — слетает после reset. Нужен default в коде.

Вывод: overshoot-фикс корректен; не-прохождение критерия при default —
следствие неверного pp, не дефект фикса.

## Требования к решению
1. `src/foc.c:196`: `#define FOC_DEFAULT_POLE_PAIRS  6` → `3`.
2. `src/foc.c:127`: комментарий `/* FOC_DEFAULT_POLE_PAIRS (6 — шильдик ACIM) */`
   → `/* FOC_DEFAULT_POLE_PAIRS (3 — шильдик «6 полюсов» = 3 пары) */`.
3. Больше ничего не менять: CLI `pp=N` (1..24), `FOC_SetPolePairs`, vf_control
   (использует `FOC_GetPolePairs()`) — без изменений. Тесты задают pp явно,
   default-тестов нет — ломаться нечему.

## Проверки
- `make && make test` — hosted + QEMU + pytest ALL PASS (без изменений набора).
- CI зелёный на ветке.
- Доп. sanity (hosted, не стенд): после `VFC_Init()` + `FOC_Init()` —
  `FOC_GetPolePairs() == 3`.

## Критерии успеха (стенд, повторный прогон пакета с pp=3 default)
1. **10/10 vf=300**: max_meas ≤ 360 RPM (ожидание 300–331), fe ≤ 35 Гц,
   vmag < 95 (ожидание ~40), 0 faults, полное окно 4100 мс.
2. Ни одного `sd2=0` (ложный FAULT_N STEVAL-2 не возникает при vmag~40).
3. Без CLI `pp=3` — default из кода (проверка `pp?`/`@FOC` после reset).
4. Старт-проверка (swing/watchdog, ≥5 положений ротора) — из пакета
   vf_fix_pkg_vf_fixes (догоняется тем же образом).

## Deliverables
1. Патч: `src/foc.c` (2 строки: define + комментарий).
2. Ветка `ai2/foc-pp3-default` от свежего origin/main, CI зелёный, приёмка.
3. Пересобранный стендовый пакет из нового main (energize + monitor-only).

## История контекста
- pp=6 заложен в аудите F1-F7 (94f7594) как «шильдик ACIM» — шильдик читали как
  «6 пар», фактически «6 полюсов» = 3 пары (стандартная маркировка АД).
- Эмпирическое подтверждение: pp=3 → цель 300 RPM и vmag 40% (стенд 01.09,
  VF_RETURN_PK2_vf_fixes_pp3.md, логи vf_fixes_10x_pp3/).
- Safety-модуль foc.c — правка только по этому ТЗ (регламент §7).

---
*ТЗ для реализации. База: origin/main @ f4fcff5. Стенд: ПК-3, 60 В, vf=300, 10×.*
