# ТЗ — Тестирование Auto-Tune v2 на реальном двигателе

## Цель

Проверить корректность алгоритмов автотюнинга: определение канала тока, измерение Rs, Ls, Isat, кривая насыщения, анализ по фазам AB/BC/CA.

## Инструменты

- Nucleo-G474RE + 2× STEVAL-IPM20B + двигатель
- Блок питания 12-48V (STEVAL)
- `run_debug_tool.bat` — GUI
- Мультиметр (для сверки Rs)
- Осциллограф (опционально, для проверки формы тока)

---

## 1. Предварительные проверки (до подключения двигателя)

### 1.1 Сборка и прошивка
```
cd C:\ST\boyler\Motor
make clean && make && make flash
```

**Ожидание:** Build OK, flash OK, Nucleo перезагружен.

### 1.2 Подключение UART
```
run_debug_tool.bat → Connect (COM15)
```

**Ожидание:** В логе `OEW FOC v0.2 @170MHz`, `ADC OK`, `PWM OK`, `> `.

### 1.3 Проверка ADC
Отправить:
```
c       (калибровка)
a       (чтение ADC)
sysinfo (проверка Vbus)
```

**Ожидание:**
- `@ADC:CAL:offset_i1=~2048:offset_i2=~1023:offset_in=~2048` — нормальные нули
- `@ADC:I1=~2048:I2=~1023:IN=~2048:VBUS=~0` — без питания VBUS=0
- `@SYS:CLK=170000000` — PLL 170 МГц ✅

---

## 2. Тест с подключенным двигателем (без STEVAL)

### 2.1 Детект обрыва фазы
Подключить Nucleo **без** STEVAL-IPM20B. Запустить `idle`:

**Ожидание:** `@IDLE:ERROR:OPEN_PHASE` на шаге с duty ≥ 20% и током < 30 мА.

### 2.2 Детект нулевого Vbus
Отключить питание STEVAL. Запустить `idle`:

**Ожидание:** `@IDLE:ERROR:VBUS_LOW` (Vbus < 12V).

### 2.3 Детект КЗ
Замкнуть фазы A-B перемычкой. Запустить `idle`:

**Ожидание:** `@IDLE:ERROR:SHORT_OR_LOW_RS` на 1-2% duty при токе > 3A.

---

## 3. Тест автоопределения канала (`ch`)

### 3.1 Базовая проверка
Подключить STEVAL + двигатель + питание (24V). Отправить `ch`:

**Ожидание (для single DC-link shunt):**
```
@AT:CH_DETECT:OK:CH=3:I=xxx:SIGN=1
```
где `CH=3` = IN, `I > 50 mA`.

**Если CH=1 (I1) или CH=2 (I2):**
- Проверить осциллографом, какой шунт реально даёт сигнал
- Возможно, плата STEVAL использует фазные шунты, а не DC-link

### 3.2 Проверка полярности
При подаче напряжения: фаза A (+) → фаза B (–), ток должен быть положительным.

**Ожидание:** `SIGN=1`.
Если `SIGN=-1` — проверить направление тока (перепутан шунт или канал).

### 3.3 Повторяемость
Запустить `ch` 5 раз.

**Ожидание:** Один и тот же канал во всех 5 запусках.
Если канал меняется — сильный шум или плохой контакт.

---

## 4. Тест Rs (сопротивление статора)

### 4.1 Multi-point Rs (`iv`)
После `ch` запустить `iv`:

**Ожидаемый вывод:**
```
@AT:RS_IV:START
@AT:RS_IV:POINT:D=2:U=480:I=85
@AT:RS_IV:POINT:D=4:U=960:I=162
@AT:RS_IV:POINT:D=6:U=1440:I=244
@AT:RS_IV:POINT:D=8:U=1920:I=325
@AT:RS_IV:POINT:D=10:U=2400:I=408
@AT:RS_IV:POINT:D=12:U=2880:I=490
@AT:RS_IV:POINT:D=15:U=3600:I=612
@AT:RS_IV:OK:Rs=xxx
```

**Проверки:**
- Ток должен расти **линейно** с duty (I = U * duty / 100 / Rs / 2)
- Если точки нелинейны — dead-time или падение на ключах
- Если ток скачкообразный — КЗ или насыщение

### 4.2 Сверка с мультиметром
Измерить сопротивление фаза-фаза мультиметром (A-B, B-C, C-A).

**Формула:** `Rs_phase = R_pp / 2`
**Ожидание:** Расхождение < 15% с `@AT:RS_IV:OK:Rs=xxx`

### 4.3 Сравнение с парным тестом
После `iv` запустить `idle`:

**Ожидание:** Rs из `idle` должен совпадать с Rs из `iv` в пределах 10%.
Если расхождение > 20% — проверить:
- duty для Rs в idle (10%) — возможно, нелинейность на малых duty
- Количество точек в iv (7 точек — достаточно)
- Offset ADC (калибровка `c`)

---

## 5. Тест Ls (индуктивность статора)

### 5.1 Запуск idle
После `ch` запустить `idle`:

**Ожидание:**
- Кривая Ls(I) должна **монотонно убывать** с ростом тока
- Максимальная Ls — при малом токе (первые точки)
- Ls падает при насыщении

**Формат вывода:**
```
@IDLE:PROG=5/50:D=5:I=244:L=629:REP=1/5
@IDLE:PROG=10/50:D=10:I=485:L=615:REP=1/5
...
@AT:STAT:Rs=850:820:890:8%:Ls=620:610:635:4%:Isat=2600:2500:2700:7%
@PARAMS:Rs=850:Ls=620:Isat=2600:...
@IDLE:CURVE:I=85,L=640:I=162,L=635:...
```

### 5.2 Проверка repeatability (5 повторов)
После `idle` запустить `stats`:

**Ожидание:** `spread < 15%` для Ls.
```
@AT:STAT:Rs=...:Ls=620:610:635:4%:Isat=...
```
- `spread = 4%` — отлично
- `spread > 15%` — проблема (шум ADC, плохой контакт, пульсации Vbus)

### 5.3 Адаптивный dt
В логе проверить `@IDLE:PROG=5/50:D=5:I=xxx:L=yyy:REP=1/5`.

Если `L=0` для малых duty → dt не хватило (di < 10 mA). Увеличить dt_us в `AT_MeasureLs_uH()`.

Если Ls падает слишком быстро → dt слишком велик (di > 500 mA, уход в насыщение).

---

## 6. Тест всех пар фаз (`pairs`)

### 6.1 Запуск
```
pairs
```

**Ожидание:**
```
@AT:PAIR:AB:Rs=850:Ls=620:Isat=0:V=1
@AT:PAIR:BC:Rs=845:Ls=615:Isat=0:V=1
@AT:PAIR:CA:Rs=860:Ls=630:Isat=0:V=1
@AT:PAIRS:OK:Rs=852:Ls=622:ASYM=1%
```

### 6.2 Проверка асимметрии
**Ожидание:** `ASYM < 10%`.

Если асимметрия > 10%:
- Плохой контакт в одной из фаз
- Межвитковое замыкание
- Несимметрия обмоток двигателя (брак)
- Неверный канал тока для BC/CA

### 6.3 Сравнение пар с idle
Rs/ls из `pairs` должны совпадать с `idle` в пределах 15%.

---

## 7. Тест Isat (ток насыщения)

### 7.1 Базовая проверка
После `idle` проверить `@PARAMS:Isat=xxx`.

**Ожидание:** Isat — точка, где Ls упала до 70% от max.
- Если Isat = 0 — max_Ls = 0 или кривая не достигла 70% падения
- Если Isat слишком мал (< 500 mA) — насыщение при малом токе (возможно, КЗ в обмотке)
- Если Isat слишком велик (> max тока теста) — увеличить max duty с 50% до 80%

### 7.2 Проверка на осциллографе
На осциллограмме тока при максимальном duty должен быть виден изгиб — точка, где рост тока ускоряется (насыщение).

Если изгиба нет — мотор не насыщается при 50% duty. Увеличить лимит.

---

## 8. Проверка GUI

### 8.1 Прогресс-бар
Запустить `idle` через GUI.

**Ожидание:** progress bar заполняется, текст `D=25% I=1200 mA L=450 uH (rep 1/5)`.

### 8.2 График кривой
После `idle` кривая должна отрисоваться на Canvas. Ожидание: синяя линия, монотонно убывающая.

### 8.3 Валидация
Ввести в поля Expected: Rs=1000, Ls=500, Isat=3000. Нажать Validate.

- Если измерено Rs=850 → `err 15% — OK`
- Если Ls=200 → `err 60% — WARN`

### 8.4 Экспорт CSV
Нажать Export CSV, открыть файл.

**Ожидание:** 3 секции — параметры со статистикой, пары фаз, кривая.

### 8.5 Abort
Запустить `idle`, через 3 секунды нажать Abort.

**Ожидание:** `@IDLE:ABORTED`, кнопки разблокированы.

---

## 9. Критерии PASS / FAIL

### PASS:
- `ch` определяет один и тот же канал в 5/5 запусках
- Rs совпадает с мультиметром в пределах 15%
- Ls монотонно убывает
- Isat > 0 и физически правдоподобен
- Асимметрия фаз < 10%
- Spread по 5 повторам < 15%
- Валидация PASS (err < 20% для всех параметров)
- GUI: прогресс, график, CSV работают

### FAIL:
- `ch` возвращает разный канал
- Rs расходится с мультиметром > 30%
- Ls растёт с током (не монотонно)
- Isat = 0 при max_Ls > 0
- Spread > 30%
- Асимметрия > 20%
- Abort не работает

---

## 10. Диагностика типичных проблем

| Симптом | Причина | Решение |
|---------|---------|---------|
| CH детект: `NO_CURRENT` | Шунт не подключён, слабый сигнал | Проверить STEVAL питание, увеличить тестовый duty до 10% |
| Rs < 50 mOhm | КЗ в обмотке или шунте | Отключить мотор, проверить мультиметром |
| Rs > 5000 mOhm | Обрыв фазы, нет контакта | Проверить разъёмы |
| Ls = 0 на всех duty | di всегда 0 (dt мал или шум) | Увеличить dt_us, проверить ADC |
| Ls растёт с I | Шум ADC, неверный канал | Проверить offset, заменить канал |
| Isat = 0 | Кривая не достигла 70% падения | Увеличить max duty до 80% |
| ASYM > 20% | Плохой контакт в одной фазе | Проверить разъёмы, прозвонить мультиметром |
| Spread > 30% | Шум ADC, пульсации Vbus | Улучшить фильтрацию, стабилизировать питание |
| Прогресс-бар не движется | Не парсится `@IDLE:PROG=` | Проверить regex в GUI |
