# Техническое задание для web AI (актуализированное)

## 1. Объект

Проект **OEW** — STM32G474RE, два инвертора (Inv1 high-side, Inv2 low-side), питаются от одной шины DC ~60 В.

Датчики тока:
- **I1** — шунт на Inv1,
- **I2** — шунт на Inv2,
- **Ires** — трансформатор тока (остаточный/нейтральный ток).

Двигатель — **асинхронный**, паспортные данные неизвестны, известно только:
- **Rs ≈ 13 Ом** на обмотку,
- **6 пар полюсов** (определено пользователем по синхронной скорости ~500 об/мин на 50 Гц).

## 2. Что уже работает

- Телеметрия ADC и VBUS корректна.
- `iv` даёт `Rs ≈ 14.1–16.7 Ом` (завышен относительно 13 Ом, см. разбор).
- `pairs` **исправлен**: многоточечная V-I регрессия дала `Rs ≈ 12.7 / 14.0 / 13.0 Ом` по фазам, `ASYM = 9%`.
- `lspos` и `oew` проходят, но для АД неинформативны; их следует игнорировать.
- `noload` **теперь стартует**: retry + повторная калибровка offset'ов в `AT_SafetyCheck` решают `NONZERO_CURRENT`.
- `rr` переписан на **lock-in детектирование** (одна амплитуда 8%, 15 периодов 5 Гц, корреляция тока с `sin`/`cos`), результат ожидается после прошивки.

## 3. Текущие проблемы

### 3.1 `iv`/`idle` дают Rs выше, чем `pairs`
- `pairs`: 13.2 Ом, ASYM 9% — **доверяем этому значению**.
- `iv`: 16.7 Ом; `idle`: 18.0 Ом.
- Вероятная причина: `iv` измеряет VBUS во время работы ШИМ и/или сильнее подвержен саге шины и шуму на одной из точек (особенно D=15%).

### 3.2 `rr` — результат неизвестен до теста
- Предыдущая версия (многоамплитудная регрессия `P(I²)`) дала `Rtotal=3.65 Ом < Rs`, то есть физически невозможный `Rr=0`.
- Новая lock-in версия отделяет активную (`Id`) и реактивную (`Iq`) составляющие тока. Если `Id` мало или фаза тока близка к 90°, `Rtotal` снова может оказаться меньше `Rs`.

### 3.3 `noload` завершается, но `Lm = 0`
- При 60V DC и V/f разгоне до 50 Гц с амплитудой 80% измерено `Ltotal ≈ 88.6 мГн`, `Irms ≈ 1.2 А`.
- Код считает `Lm = Ltotal - Ls`, где `Ls` взят из `pairs`/`idle` (~90–175 мГн). Поэтому `Lm` получается 0.
- Проблема определений: то, что измеряют `pairs`/`idle` как `Ls`, для АД ближе к **полной индуктивности статора** (`Lσ + Lm`), а не к индуктивности рассеяния. Нужна методика разделения `Lσ` и `Lm`.

### 3.4 `oew` и `lspos`
- Для асинхронного двигателя неприменимы. `oew` даёт ~1.5 Гн (артефакт насыщения/шума), `lspos` даёт разброс >200%.

## 4. Что нужно получить

1. **Валидация / доработка `rr`**: получить `Rtotal > Rs` и ненулевой `Rr`.
2. **Методика получения `Lσ`, `Lm`, `Lr`, `Tr`** при ограниченном Vbus ≈60 В (≈15% от номинального напряжения двигателя).
3. **Корректная формула `noload`**, которая отдаёт положительный `Lm`.
4. **Пошаговый план запуска FOC** на этом АД с известными `Rs`, `pp=6` и ограниченным Vbus.

## 5. Ограничения

- Код на **CMSIS only**, HAL запрещён.
- Изменения вносятся в `src/autotune.c`, `src/autotune.h`, `main.c`.
- После любого изменения: `make` → `make flash`.
- Датчик тока **один** (I1/I2/Ires — выбран автоматически), фазные токи напрямую не измеряются.
- Энкодера/резольвера нет; число пар полюсов задаётся вручную (`pp=6`).
- Доступные логи:
  - `logs/test_log_20260805_223917.log` — до lock-in.
  - `logs/test_log_20260805_231729.log` — после патча (pairs, noload OK, rr до lock-in).

## 6. Вопросы к AI

1. **Как интерпретировать upcoming lock-in `rr`?** Если `Id` мал, а `Iq` доминирует (φ ≈ 90°) — это признак того, что 5 Гц слишком низко для АД, или методика lock-in на одном датчике тока всё равно не корректна? Как правильно измерить `Rr` при одном датчике тока на DC-шине?
2. **Как получить `Lm`/`Lr`/`Tr` без паспортных данных и при Vbus=60 В?**
   - Как отделить `Lσ` от `Lm`, зная, что `pairs`/`idle` измеряют полную индуктивность статора?
   - Как пересчитать параметры, если номинальное напряжение двигателя неизвестно, но доступно только ~15% от него?
   - Можно ли запустить FOC с приближёнными `Lm`/`Lr`/`Tr` и уточнить их уже при работе?
3. **Корректная формула `noload`**:
   - Почему `Ltotal` получается меньше `Ls`?
   - Какую величину следует вычитать из `Ltotal` — `Ls` из `pairs`, `idle`, или какую-то долю от неё?
4. **FOC-запуск**:
   - Какой минимальный набор параметров достаточен (`mp=<Rs>,<Ls>,<Rr>,<Lm>,<Tr>,<Ke>,<pp>,<J>`)?
   - Как масштабировать `Ls`, `Lm`, `Tr` при работе от 60 В, если номинал неизвестен?
   - Нужно ли вообще `Rr` точный для sensorless FOC с обсервером, или достаточно порядка?
5. **Если lock-in `rr` не сработает**, какой альтернативный тест предложить (DC-инжекция на заблокированном роторе, разгон под V/f с измерением скольжения и т.д.)?

## 7. Ожидаемый результат

- Конкретные изменения в коде (diff) для `rr`/`noload`/`autotune.h`, если нужны.
- Чёткая методика получения `Rr`, `Lσ`, `Lm`, `Lr`, `Tr` для этого АД с текущим железом.
- Пошаговая инструкция: какие команды ввести, какие значения взять для `mp=...`, что ожидать в логах.
