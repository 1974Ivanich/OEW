# Техническое задание для web AI

## 1. Объект

Проект **OEW** — STM32G474RE, два инвертора (Inv1 high-side, Inv2 low-side), питаются от одной шины DC ~60 В.

Датчики тока:
- **I1** — шунт на Inv1,
- **I2** — шунт на Inv2,
- **Ires** — трансформатор тока (ostаточный/нейтральный ток).

Двигатель — **асинхронный**, паспортные данные неизвестны, известно только:
- **Rs ≈ 13 Ом** на обмотку.

## 2. Что уже работает

- Телеметрия ADC и VBUS корректна.
- Команда `iv` (измерение Rs по V-I кривой) даёт `Rs ≈ 12.8–14.1 Ом`, что близко к 13 Ом.
- `pairs` теперь проходит по всем трём фазам (раньше падая с `OPEN_PHASE` на Ires).
- `lspos` больше не падает с `BAD_CURRENT`.

## 3. Текущие проблемы

### 3.1 `pairs` даёт сильно завышенный Rs и большую асимметрию
- Пример: A=20.5 Ом, B=22.2 Ом, C=27.3 Ом, ASYM=29%.
- Причина: метод считает `R = U/I` по **одной** точке, и в `U` оказывается неучтённое падение на транзисторах, диодах и dead-time.
- `iv` даёт правильный результат, потому что строит линейную регрессию `U(I)` и получает тангенс — это и есть Rs.

### 3.2 `rr` (измерение Rr на заблокированном роторе, 5 Гц) даёт физически невозможный результат
- На амплитуде 20% падал с `OVERCURRENT I≈10 А`.
- После уменьшения амплитуды до 8% перетока нет, но: `Rtotal=8.1 Ом`, что **меньше** `Rs≈13 Ом`. В итоге `Rr=0` и предупреждение `RR_LESS_THAN_RS`.
- Метод в коде считает активную мощность как `P = Σ(u_inst · i_ma)` и `I² = Σ(i_ma²)`, а затем `R = P/I² · 1000`. Проблема: измеряется ток только **одним** датчиком (I1/I2), который стоит на шине DC, а не фазный ток. Для синусоидального 3-фазного напряжения на заблокированном роторе это даёт не тот «active» ток.

### 3.3 `noload` не стартует
- Падает с `NONZERO_CURRENT:Ires=169 мА` после `rr`.
- Это остаточный сдвиг нуля датчика после большого тока в `rr`.
- В код добавлена повторная калибровка offset'ов по команде `f` (fault clear), но проблема может сохраниться, если ток не успел спасть.

### 3.4 `oew` и `lspos` дают неадекватные Ls
- `oew`: `Ls=1508129 мГн` (~1.5 Гн), потому что на больших duty ток падает до 12 мА и формула `L = U·dt/dI` взрывается.
- `lspos`: разброс Ls 92%, медиана 17.7 мГн. Для асинхронного двигателя этот тест неприменим — он предназначен для определения полюсов PMSM.

## 4. Что нужно получить

1. **Надёжный Rs** — уже почти решено через `iv`, но нужно убедиться, что он стабильно даёт ~13 Ом.
2. **Корректный Rr** для асинхронного двигателя при неизвестных паспортных данных.
3. **Корректный Ls/Lm** (или хотя бы приближение), чтобы можно было запустить FOC/обсервер.
4. **Работающий `noload`** для измерения магнитизации.
5. **Определение числа пар полюсов** без шильдика (или чёткое указание, как его задать вручную).

## 5. Ограничения

- Код на **CMSIS only**, HAL запрещён.
- Изменения вносятся в `src/autotune.c`, `src/autotune.h`, `main.c`.
- После любого изменения прошивка заливается через `make` → `make flash`.
- Доступны логи в `logs/`. Последние:
  - `logs/test_log_20260805_222142.log`
  - `logs/test_log_20260805_223917.log`

## 6. Вопросы к AI

1. Почему `pairs` завышает Rs и как переделать его на многоточечную регрессию (как `iv`) с корректным расчётом U для каждой пары?
2. Почему метод `rr` через `P = Σ(u·i)` даёт `Rtotal < Rs`? Как правильно измерить `Rr` асинхронника с одним датчиком тока на шине DC? Может, заменить на DC-шаг или на управляемый по току низкочастотный синус?
3. Как защитить `noload` от остаточного offset'а после `rr`? Достаточно ли паузы + калибровки или нужна другая логика?
4. Как для АД без шильдика определить число пар полюсов и `Lm`/`Lr`/`Tr`? Какие команды и методы использовать, а какие исключить (`lspos`, `oew`)?
5. Дай пошаговый план, какой минимальный набор параметров достаточно задать вручную (`pp=N`, `mp=...`), чтобы запустить FOC на этом АД.

## 7. Ожидаемый результат

- Конкретные изменения в коде (diff) или объяснение, почему текущая архитектура не позволяет решить задачу.
- Пошаговая инструкция для пользователя: что прошить, какие команды ввести, как интерпретировать вывод.
