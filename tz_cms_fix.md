# ТЗ: Исправить режим center-aligned таймеров TIM1/TIM8 (CMS=10 → CMS=11)

Проект: FOC-двигатель на STM32G474RE, CMSIS-only (без HAL), компиляция ARM GCC через make.

## Проблема
В `src/pwm.c` таймеры TIM1 и TIM8 настроены с `TIM_CR1_CMS_1` (center-aligned mode 2).
Измерение осциллографом (sigrok, 8 МГц) показало: при заданном duty 15% на пине реально 7.5%
(импульс = CCR тиков, а не 2×CCR). Причина: в mode 2 (CMS=10) выход активен только на
одном склоне счёта. Нужен mode 3 (CMS=11) — активен на обоих склонах → полный duty.

## Задача
1. В `src/pwm.c` (строки ~87 и ~101) изменить инициализацию CR1 обоих таймеров:
   добавить бит `TIM_CR1_CMS_0` (CMS_1|CMS_0 = mode 3), сохранив `TIM_CR1_ARPE`.
2. Проверить, что макрос `TIM_CR1_CMS_0` определён в
   `Drivers/CMSIS/Device/ST/STM32G4xx/Include/stm32g474xx.h`.
3. Проверить влияние на dead-time (BDTR/DTG) и на FOC-ядро (foc.c использует свой ARR=999):
   смена CMS не должна ломать запуск/синхронизацию TIM8 (SMCR=Reset от TIM1 TRGO).
4. Пересобрать: `make` — без ошибок и предупреждений, бинарь `build/firmware.bin`.

## Ограничения
- Только изменение режима CMS в двух строках инициализации. Ничего больше не трогать
  (не менять PSC/ARR/DTG/CCER/BDTR, не добавлять магические числа).
- Не менять поведение FOC-ядра: только инициализация таймеров.
- Комментарий в коде обновить: указать mode 3 и почему (полный duty на обоих склонах).

## Критерий приёмки
- Сборка без ошибок.
- После прошивки (STM32_Programmer_CLI) тест PWM duty=15% на пинах PC0/PC1/PC2 (TIM1)
  показывает 15% ±1% (не 7.5%) при частоте 50 кГц.
- FOC запускается штатно, dead-time не изменился (1.5 мкс).

## Ответ
Дай только: изменённые строки кода (2 шт), подтверждение наличия макроса CMS_0,
краткий отчёт о влиянии на dead-time/FOC, команду сборки и ожидаемый результат.
