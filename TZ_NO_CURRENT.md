# ТЗ: Диагностика ошибки `@AT:CH_DETECT:ERROR:NO_CURRENT`

## 1. Цель

Проанализировать причину ошибки автоопределения канала тока и выдать
ранжированный список наиболее вероятных причин + конкретный план
диагностики (аппаратной и программной).

## 2. Контекст

- **MCU:** STM32G474RE (NUCLEO-G474RE, Cortex-M4F, 170 МГц)
- **Прошивка:** OEW FOC (Open-End Winding, CMSIS, без HAL) — каталог `c:\ST\boyler\Motor`
- **Топология:** двигатель подключён между **двумя** инверторами STEVAL-IPM20B
  (Inv1 — TIM1, Inv2 — TIM8). Оба инвертора на общей DC-шине.
- **Токовые датчики:** one-shunt на каждый инвертор:
  - I1 (фаза A, Inv1) — PA0 / ADC2_IN1
  - I2 (фаза B, Inv2) — PA1 / ADC2_IN2
  - Ires (нулевой ток, трансформатор) — PA6 / ADC2_IN3
- **Vbus:** PC4 / ADC2_IN5, делитель 1:125
- **UART-диагностика:** 115200 бод, команда `ch`, телеметрия `@AT:...`, `@DBG:...`
- **ШИМ:** 5 кГц, center-aligned, TIM1 (Inv1: PC0/PC1/PC2 + PA7/PB0/PB1),
  TIM8 (Inv2: PC6/PC7/PC8 + PC10/PC11/PC12), dead-time ~1 мкс
- **Enable:** EN1 = PB4, EN2 = PB5

## 3. Симптом

При отправке команды `ch` в Nucleo Debug Tool:

```
Starting Nucleo Debug Tool...
[Sigrok] Scan: False
[AT] [AT] Sent 'ch', waiting...
[AT] [AT] Error: @AT:CH_DETECT:ERROR:NO_CURRENT
[AT] [AT] Error: @AT:CH:FAIL
```

## 4. Что делает команда `ch` (логика Autotune_DetectChannel)

Файл: `src/autotune.c`, функция `int8_t Autotune_DetectChannel(void)` (строки 94–168).

Последовательность:

1. `UART_SendStr("@AT:CH_DETECT:START")`
2. Если FOC работает — остановка (`FOC_Stop()`)
3. `NVIC_DisableIRQ(ADC1_2_IRQn)` — запрет прерываний АЦП
4. `PWM_Disable()` — выключение ШИМ
5. `ADC_CalibrateOffsets()` — калибровка нулей АЦП
6. `PWM_SetDuty1(0,0,0)`, `PWM_SetDuty2(0,0,0)` — нулевые duty
7. `both_enable()` — включение **обоих** инверторов (EN1=PB4, EN2=PB5, TIM1+TIM8 CEN/MOE)
8. **Измерение «нуля»:** `i1_zero`, `i2_zero`, `in_zero` (мА)
9. Подача тестового импульса: `PWM_SetDuty1(5, 0, 0)` — **5% duty** на фазу A Inv1, `delay_us(300)`
10. **Измерение «теста»:** `i1_test`, `i2_test`, `in_test` (мА)
11. Снятие: `PWM_SetDuty1(0,0,0)`, `PWM_SetDuty2(0,0,0)`, `both_disable()`, `NVIC_EnableIRQ(ADC1_2_IRQn)`
12. Расчёт дельт:
    ```c
    d_i1 = |i1_test - i1_zero|
    d_i2 = |i2_test - i2_zero|
    d_in = |in_test - in_zero|
    ```
13. Выбор канала с максимальной дельтой (`best = max(d_i1, d_i2, d_in)`)
14. **Условие ошибки:**
    ```c
    if (best < 30) {   // порог 30 мА
        UART_SendStr("@AT:CH_DETECT:ERROR:NO_CURRENT\r\n");
        return -1;
    }
    ```

При этом по UART **до** ошибки должны быть диагностические строки:
- `@DBG:CH:EN:GPIOB_ODR=...:TIM1_CR1=...:TIM1_BDTR=...:TIM1_CCER=...:TIM8_CR1=...:TIM8_BDTR=...:TIM8_CCER=...`
- `@DBG:CH:CCR:TIM1_CCR1=...:TIM1_ARR=...:TIM8_CCR1=...:TIM8_ARR=...`
- `@DBG:CH:ZERO:raw_i1=...:raw_i2=...:raw_ires=...:raw_vbus=...`
- `@DBG:CH:ZERO:mA:i1=...:i2=...:ires=...:vbus=...mV`
- `@DBG:CH:DUTY:TIM1_CCR1=...:TIM1_CNT=...:TIM1_CR1=...`
- `@DBG:CH:TEST:raw_i1=...:raw_i2=...:raw_ires=...:raw_vbus=...`
- `@DBG:CH:TEST:mA:i1=...:i2=...:ires=...:vbus=...mV`
- `@DBG:CH:DELTA:d_i1=...:d_i2=...:d_ires=...`

## 5. Диагностические данные, которые нужно запросить у пользователя

Чтобы дать точный ответ, web-AI должен попросить пользователя прислать **полный лог** команды `ch`
включая все строки `@DBG:CH:...` (особенно `ZERO:mA`, `TEST:mA`, `DELTA`,
`EN`, `CCR`, `DUTY`), и ответить на вопросы:

1. Есть ли напряжение на DC-шине? Какое (например 24/48 В)?
2. Подключён ли двигатель к обоим инверторам? Схема OEW (6 концов обмоток)?
3. Закорочен/заблокирован ли ротор?
4. Горят ли индикаторы/нет ли FAULT на STEVAL-IPM20B?
5. В каком положении перемычки на STEVAL-IPM20B (SW5/SW6 замкнуты? SW1/SW2/SW4 в 1-2?)
6. Что показывают `@DBG:CH:ZERO:raw...` и `@DBG:CH:TEST:raw...` — есть ли разница в кодах АЦП?
7. Что показывают `@DBG:CH:EN:...` — взведены ли `TIM1_CR1.CEN`, `TIM1_BDTR.MOE`, `TIM1_CCER.CC1E/CC1NE`, `GPIOB_ODR` (EN1/EN2)?
8. Что показывает `@DBG:CH:DUTY:TIM1_CCR1` — равно ли ~5% от ARR (ARR=99 → CCR1≈5)?

## 6. Задача для AI

### 6.1. Анализ причин
Составить **ранжированный список** наиболее вероятных причин ошибки
`NO_CURRENT` с обоснованием каждой. Рассмотреть как минимум:

1. **Отсутствие силовой шины Vbus** (0 В или <~8 В при 5% duty ток <30 мА).
   Расчёт: при 24 В и 5% duty среднее U ≈ 1.2 В. Если Rs обмотки + Rds+диод ≈ 1 Ом,
   установившийся ток ≈ 1.2 А, но при большой Ls и 300 мкс нарастание мало:
   dI = U·dt/L = 1.2·0.0003 / 0.01 = 36 мА ≈ порог. При 12 В и L=20 мГн → 9 мА → гарантированный FAIL.
2. **Обрыв фазы двигателя / неверное подключение** (нет контура для тока).
3. **Не доходит Enable** (EN1/EN2): GPIOB ODR, защита IPM (FAULT), EM_STOP.
4. **ШИМ TIM1 не запущен корректно** — CEN/MOE/CCER не выставлены, CCR1=0, dead-time.
5. **Калибровка АЦП «съела» сигнал** — `ADC_CalibrateOffsets()` делает замер смещения
   в момент, когда на инверторы уже подано питание/разрешение → смещение неверное
   (логические единицы шины через паразитные цепи), и реальный ток маскируется.
6. **Порог 30 мА слишком высок** для маломощного двигателя / высокоиндуктивной обмотки.
7. **Неверный канал/вывод АЦП** (I1/I2/Ires не на тех пинах, неверный индекс ADC2).
8. **Шунт/усилитель не подключён** (перемычки SW5/SW6/SW7/SW8, SW1/SW2/SW4, TSO).
9. **Защита FAULT активна** — TIM заблокирован (MOE сброшен).
10. **Короткое замыкание/перегрузка** — сработала защита до момента замера.

### 6.2. Дерево решений
Построить пошаговое дерево диагностики: от самых дешёвых проверок
(мультиметр, визуальный осмотр, лог `@DBG`) к более сложным (осциллограф,
проверка сигналов HIN/LIN, замер на шунте).

### 6.3. Рекомендации по доработке кода (опционально)
Предложить минимальные изменения в прошивку, улучшающие диагностику или
устойчивость теста, например:

- Увеличить duty теста с 5% до 10–15% и/или увеличить `delay_us(300)` до 1–5 мс
  (с контролем по нарастанию тока), чтобы гарантированно превысить порог.
- Снизить порог `best < 30` до `best < 10` (или ввести адаптивный порог).
- Добавить проверку Vbus в начале `Autotune_DetectChannel()` (`@DBG:CH:VBUS=...mV`).
- Добавить отдельные `@DBG:CH:MEAS:` с замером после 1 мс и 5 мс, чтобы видеть кривую нарастания.
- Вызывать `AT_SafetyCheck()` перед подачей напряжения.
- Проверить порядок `ADC_CalibrateOffsets()` и `both_enable()` (калибровать при выключенных, потом включать; или калибровать после стабилизации).
- Добавить чтение регистра защиты TIM (`TIM1->SR`, `TIM1->BDTR.MOE`, `GPIOB->IDR` для FAULT) и вывод `@DBG:CH:FAULT=...`.
- Добавить проверку, что `TIM1->CCR1` действительно стал ~5% от ARR перед замером.

### 6.4. Ожидаемый результат
Ответ должен содержать:

1. **Вердикт**: наиболее вероятная причина (или 2–3 равновероятные) с расчётами.
2. **Чек-лист проверок** в порядке выполнения (с ожидаемыми значениями).
3. **Какие данные из лога `@DBG` критичны** и как их интерпретировать
   (например: если `raw_i1` в TEST > ZERO на N кодов → ток есть, проблема в пороге;
   если `TIM1_BDTR.MOE=0` → защита; если `GPIOB_ODR` бит 4=0 → EN не выставлен).
4. **Конкретные правки кода** (фрагменты на C) если причина в прошивке.

## 7. Файлы проекта для анализа

- `src/autotune.c` — функция `Autotune_DetectChannel()` (строки 94–168), `AT_SafetyCheck()` (193–221), `both_enable/both_disable`
- `src/adc.c` / `src/adc.h` — `ADC_CalibrateOffsets()`, `ADC_GetI1_mA()`, `ADC_GetI2_mA()`, `ADC_GetIres_mA()`, `ADC_GetVbus_mV()`, `ADC_GetRawI1/I2/Ires/Vbus`
- `src/pwm.c` / `src/pwm.h` — `PWM_SetDuty1()`, `PWM_SetDuty2()`, `PWM_Disable()`, `PWM_GetARR()`
- `main.c` — обработчик команды `ch`
- `nucleo_debug_tool.py` — GUI-инструмент, отправка `ch`, парсинг `@AT:CH_DETECT*`
- `OEW_FOC_DOC.md` — схема подключения стенда, перемычки STEVAL-IPM20B, параметры датчиков

## 8. Критерии приёмки ответа AI

- [ ] Причины отсортированы по вероятности с обоснованием.
- [ ] Учтены данные `@DBG:CH:*` лога (их нужно запросить и интерпретировать).
- [ ] Есть пошаговый план аппаратной проверки (мультиметр/осциллограф).
- [ ] Есть конкретные правки кода (если причина программная), с учётом ограничений: **только CMSIS**, никакого HAL.
- [ ] Ответ на русском языке.