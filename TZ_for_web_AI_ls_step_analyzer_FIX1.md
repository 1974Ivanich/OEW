# ТЗ-ФИКС №1 для web AI: `tools/ls_step_analyze.py` — падение на реальном логе

Ветка-исполнитель: продолжение `ai2/tz-ls-step-analyzer` (PR #20).
Проверено ПК-2 на реальном стендовом логе. Ветка НЕ принята; ниже — что именно исправить.

## 1. Что проверено и что совпало

```text
SHA 447255776156932956948386630b61af828d14de  = заявленному ✓
дифф против origin/main: ровно 3 файла (TZ, tests/test_ls_step_analyze.py, tools/ls_step_analyze.py),
  запрещённых путей (src/**, main.c, Makefile, *.ioc, tests/*.c, tests/mocks/**) нет ✓
PR #20: OPEN, draft, MERGEABLE ✓
CI run 35430647800: success, headSha совпал ✓
py -3 -m pytest tests/test_ls_step_analyze.py -q   → 29 passed ✓ (в самом файле 29 тестов; 482 —
  это весь набор проекта, в отчёте это было слито в одну цифру)
JSON пишется ДО печати ✓ (требование ТЗ выполнено — при падении артефакт на месте)
математика на реальных данных совпала с прошивкой: all_pairs медиана d=5 = 9435.3 мкГн
  против Lstep_uH=9393 в логе ✓
```

## 2. Дефект (блокирующий): падение на реальном логе

Воспроизведение (лог стенда: `ls_step.log`, 60 кадров, три уровня, `n_valid = 3/3/1`):

```bash
py -3 tools/ls_step_analyze.py "C:/campaign_raw/ls_step_return_20260919_c/logs/ls_step.log" --json out.json
```

Фактически:

```text
Traceback (most recent call last):
  File "tools/ls_step_analyze.py", line 332, in main
    print(render(report,ns.log))
  File "tools/ls_step_analyze.py", line 301, in render
    lines.append(f"  fit: L_fit={fit['L_fit_uH']:.2f} uH tau={fit['tau_us']:.2f} us ...")
                                 ~~~^^^^^^^^^^^^
TypeError: 'NoneType' object is not subscriptable
```

Причина: `fit` для уровня может быть `None` (в JSON по реальному логу `fit=None` на всех трёх
уровнях и `cond=None`), но `render()` подставляет его без проверки. Код возврата `1` в этом
случае **случайно совпал** с `UNUSABLE` — фактически это неисправленное исключение, а не
вердикт; для автоматизации это опаснее, чем явная ошибка.

При этом в JSON анализ уже корректен:

```json
"verdict": "UNUSABLE",
"checks": {"PWM_PERIOD": "PASS", "SIGN": "PASS", "SHAPE": "FAIL",
           "CONVERGENCE": "FAIL", "PLAUSIBILITY": "FAIL:OUT_OF_RANGE",
           "CONDITIONING": "FAIL:INSUFFICIENT_TRANSIENT"},
"levels": [{"d": 5, "fit": null, "cond": null, "all_pairs": {"median_lin_uH": 9435.3, ...}}, ...]
```

## 3. Что требуется сделать

1. **`render()` не должен падать ни на одном входе.** Для `fit is None` печатать явную строку,
   например: `fit: НЕДОСТУПЕН (причина: <reason>)` — и в JSON добавить `"fit_unavailable_reason"`.
2. **`cond is None`** (fit недоступен) — печатать `обусловленность: НЕДОСТУПНА (нет фита)` и
   в отчёте явно указывать, что `PLAUSIBILITY`/`CONVERGENCE` оценивались без фита.
3. **Коды возврата — только по вердикту**: 0 = USABLE, 1 = UNUSABLE/INCOMPLETE,
   2 = UNREADABLE. Никакое исключение не должно давать эти коды. Любая внутренняя ошибка —
   `2` с печатью `@ERROR:` строки в stderr и валидного JSON (с `verdict: "UNREADABLE"` и
   `reasons: ["INTERNAL_ERROR:<тип>"]`), без traceback как основного вывода.
4. **Причина недоступности фита должна быть названа.** В нашем случае ряд ступени — медленный
   спад (отрицательные значения, знак против приложенного напряжения), и фит первого порядка
   не может быть построен. Требуется явное `reason`, например:
   `FIT_NO_POINTS` / `FIT_NONPOSITIVE_ISS` / `FIT_DEGENERATE` — какое условие не выполнено.
5. **Регрессионный тест на этот класс.** Добавить фикстуру, повторяющую форму реального лога
   (ряд ступени с отрицательными значениями и спадом; `all_pairs` может дать медиану,
   а фит — `None`), и проверять: (а) инструмент не падает; (б) печатается строка про
   недоступный фит; (в) вердикт `UNUSABLE` и причины в `reasons`. Синтетика «правильного»
   экспоненциального нарастания этот класс не покрывает — именно поэтому дефект и прошёл.
6. **`SIGN` — считать по СЫРОМУ ряду ступени, а не только по допущенным парам.** На реальном
   логе `SIGN=PASS` (по 3 принятым парам d=5), хотя сырой ряд знакопеременный
   (`dI = +70, −6, +13, −19, +63, −25, +19, +64, −51, …`). Требуется печатать и оценивать
   отдельную проверку `SIGN_RAW` по всем парам `ph=1`; `SIGN` по допущенным парам оставить.
   Это не меняет вердикт нашего прогона (`SHAPE`/`CONVERGENCE`/`CONDITIONING` уже FAIL),
   но убирает ложный зелёный флаг у сломанного измерения.

## 4. Правка самого ТЗ (внести тем же коммитом)

В `TZ_for_web_AI_ls_step_analyzer.md` §4.3 формула смещения указана неверно для формулы §4.1
(это подтверждено и нашим разбором, и реализацией). Заменить блок про `θ/(1 − e^(−θ))` на:

```text
Для двухточечной оценки с вычитанием сопротивления по середине интервала (§4.1) смещение равно
    L_lin / L = (x/2)·coth(x/2) ≈ 1 + x²/12,   где x = dt/τ = 1/cond,
то есть при cond ≥ 3 смещение < 1 %, при cond = 1 ≈ +8 %, при cond = 0.5 ≈ +31 %,
при cond = 0.31 ≈ +75 % (проверенные точки: cond=10 → +0.1 %, 5 → +0.3 %, 3 → +0.9 %,
2 → +2.1 %, 1 → +8.2 %, 0.5 → +31.3 %, 0.31 → +74.6 %, 0.2 → +153 %, 0.1 → +400 %).
Формула θ/(1−e^(−θ)) (θ = dt/τ, без поправки по середине интервала) к §4.1 НЕ относится — это
смещение некорректированной оценки U·dt/ΔI; в ТЗ её быть не должно.

Наблюдение исполнителя для L = 8000 мкГн, Rs = 13 Ω, PWM = 200 мкс (+0.9 % вместо ожидаемых
по прежней формулировке +6…18 %) — ВЕРНО и соответствует выведенной выше формуле:
x = 0.325, (x/2)·coth(x/2) = 1.0088.
```

Пороги `cond` в §4.3 сохранить, но привязать к новой формуле:
`cond ≥ 3` (< 1 %), `1 ≤ cond < 3` (+1…8 %), `0.5 ≤ cond < 1` (+8…31 %), `cond < 0.5` → FAIL.

## 5. Что НЕ делать

- Не менять `src/**`, `main.c`, `Makefile`, `*.ioc`, `tests/*.c`, `tests/mocks/**`.
- Не подставлять значения для `mp=` и не выдавать «Ls» — семантика `L_fit` остаётся
  диагностической оценкой измерения.
- Не «подгонять» фит под реальный ряд руками (например, добавляя сдвиг/модуль) — если модель
  не описывает данные, это должно быть вердиктом, а не подкруткой.

## 6. Критерии приёмки фикса (проверяет ПК-2)

1. `py -3 tools/ls_step_analyze.py <реальный лог> --json out.json` — не падает, код 1,
   в stdout явная строка про недоступный фит, в `reasons` — все причины прогона.
2. `py -3 -m pytest tests/test_ls_step_analyze.py -q` — зелёный, набор вырос (≥ 31).
3. Дифф: только разрешённые файлы; изменение ТЗ §4.3 ровно по тексту выше.
4. Повторный прогон на том же логе даёт идентичный JSON (детерминизм).
