# TZ_VF_OVERSHOOT_FIX — ограничение перелёта V/f (f_e от рампы, а не от measured)

## Цель
Устранить перелёт V/f: мотор разгоняется до ~1200 RPM при цели 300 RPM (open-loop
перелёт, зафиксирован на стенде 01.09, кампании v7/vf_mon10x_3: max_meas 1132–1274
при vf=300). Причина — **электрическая частота строится от измеренной скорости
ротора, а не от заданной (рамповой)**, из-за чего контур самоподдерживает любую
скорость и не может вернуть мотор к цели.

## Условия
- Железо: STM32G474RE + 2× STEVAL-IPM20B, DC-link 60 В, мотор в цепи (pp=6 на стенде).
- Сборка стенда: `make clean && make EXTRA_CFLAGS="-DOEW_HS1_COMMISSIONING_RELEASE=1 -DOEW_SD_MONITOR_ONLY=1"`
  (монитор-режим — рабочий для V/f-трека, принят в main fbce551).
- База: `origin/main` (fbce551, приёмка SD monitor-only).
- UART: `@VFLOG` (t/meas/fe/fslip/vmag/vbus/sd1/sd2), команды `vf=300`, `vf=0`, `p?`.
- Логи перелёта: `C:\campaign_raw\vf_mon10x_3\attempt_1..10.log` (fe до 122 Гц, vmag=95).

## Проблема (баг-репорт, точное место)
`src/vf_control.c:204-206` (VFC_Update, 1 кГц):
```c
{ int32_t pp = FOC_GetPolePairs(); if(pp < 1) pp = 1;
  vfc.f_e_hz = (int32_t)(((int64_t)pp * vfc.measured_rpm) / 60) + vfc.f_slip_hz;
  vfc.f_e_hz = CLAMP(vfc.f_e_hz, -VFC_MAX_FE_HZ, VFC_MAX_FE_HZ); }
```
**f_e = pp·measured/60 + slip.** Следствие: f_e всегда соответствует фактической
скорости → синхронная скорость = measured (с точностью до slip) → контур не
создаёт тормозящего момента при перелёте. slip-PI (Kp=0.005 Гц/rpm, Ki=0.02 Гц/rpm/с,
насыщение ±5 Гц) даёт максимум ±50 RPM коррекции (pp=6), что не возвращает мотор
с 1200 на 300. Плюс: пока ramp > measured, интегратор slip копится в +5 Гц —
мотор ускоряется сверх рампы (саморазгон).

**Механизм (подтверждён логами):** при vf=300, pp=6: ramp=300 → синхронная 30 Гц.
Пока мотор отстаёт, slip=+5 → f_e=35 Гц → мотор едет к 350 RPM и выше; по мере
разгона f_e = pp·measured/60 + slip следует за measured (fe=122 Гц при meas=1274:
6·1274/60−5≈122 ✓) — мотор застревает на высокой скорости, vmag уходит в
насыщение 95%. Обратной силы нет.

## Требования к решению

### 1. Формула f_e — от рамповой (заданной) скорости (основной фикс)
`src/vf_control.c:205`: заменить `measured_rpm` на `ramp_current_rpm`:
```c
vfc.f_e_hz = (int32_t)(((int64_t)pp * vfc.ramp_current_rpm) / 60) + vfc.f_slip_hz;
```
- f_e = синхронная рампы + slip. При перелёте (measured > ramp): slip → −5 Гц,
  f_e = pp·ramp/60 − 5 → синхронная ниже рампы → мотор тормозит к цели.
- При отставании (measured < ramp): slip → +5, f_e чуть выше синхронной рампы →
  подтягивает. Максимальный перелёт ограничен ±5 Гц slip = ±50 RPM (pp=6) — вместо 1200.
- CLAMP на VFC_MAX_FE_HZ (200) оставить.
- vmag-характеристика не меняется (`100·|f_e|/rated + boost`, clamp 95) — при
  vf=300 f_e≤35 Гц → vmag≤85%, насыщение 95% больше не достигается на цели.

### 2. Анти-windup интегратора slip (опционально, рекомендуется)
`vfc_speed_pi()` (src/vf_control.c:29-36): интегратор копится в +5 Гц на всём
разгоне (пока measured < ramp). С новой формулой это безопасно (f_e ограничена),
но даёт задержку схождения. Рекомендуется условное интегрирование:
не накапливать интегратор, когда выход уже на насыщении И ошибка того же знака
(back-calculation или conditional integration). Если меняете — обновить hosted-тесты
(slip-PI тесты в tests/vf_control_test.c: «slip: |f_slip| ≤ 5 Hz», «physical slip PI»).

### 3. Тесты (hosted, tests/vf_control_test.c)
Добавить минимум 2 проверки (не ломая существующие — они используют measured==ramp):
- **«overshoot: f_e следует за рампой, не за measured»**: pp=2, target=300,
  ramp_current=300, measured=1200, run 1 тик → f_e == 2·300/60 + slip(300−1200→−5)
  = 10 − 5 = 5 Гц (НЕ 2·1200/60=40 Гц). NEAR(f_e, 5, 1).
- **«overshoot: f_e ограничена рампой + max slip»**: pp=2, ramp_current=300,
  measured=0 → f_e == 10 + 5 = 15 Гц (не растёт с measured).
- Прогнать: `make test` (hosted + QEMU + pytest) — все зелёные, включая 26 V/f-проверок QEMU.

## Критерии успеха (стенд, vf=300, pp=6)
1. **max meas ≤ 360 RPM** (300 + 50 RPM допуска на slip/инерцию) во всех 10 попытках
   (было 1132–1274). В vf_bootstrap_10x.py добавить проверку `max_meas <= target+60`.
2. **fe ≤ 35 Гц** на установившемся режиме (было до 122 Гц); vmag < 95 (не в насыщении).
3. 10/10 стартов, ни одного fault (FAULT_R=18 / STOPPED) — как в vf_mon10x_3.
4. `make test` зелёный; CI зелёный на ветке.

## Deliverables
1. Патч: `src/vf_control.c` (строка 205, опц. анти-windup), `tests/vf_control_test.c`.
2. Локально: `make && make test` PASS, commissioning-сборка PASS.
3. Ветка `ai2/vf-overshoot-fix` от свежего `origin/main` + CI зелёный.
4. Стендовая кампания 10× vf=300 (60 В, monitor-only образ) с max_meas ≤ 360.

## История контекста
- Перелёт известен с v6/v7 (01.09.2026): open-loop V/f без скоростного регулятора,
  «fe до 122 Гц, vmag=95 насыщение» — считался «не-багом», но это следствие формулы
  f_e от measured: контур не имеет возвращающей силы (синхронная = measured ± slip).
- Текущая структура slip-PI (физический, Kp=328 Q16=0.005 Гц/rpm, Ki=1311) —
  из пакета vf-slip-pi-physical (bc85572), НЕ менять коэффициенты без отдельного ТЗ.
- База: origin/main @ fbce551; образ стенда: monitor-only (SD monitor, BKE=0) —
  пакет C:\campaign_raw\vf_fix_pkg_sd_monitor_20260901\.

## Примечания
- Safety-модуль vf_control.c — правка ТОЛЬКО по этому ТЗ (регламент §7).
- Не менять vf_start.c (используется FOC), не менять контракт VfcApertureContract.
- Реверс/отрицательные скорости: формула от ramp_current работает симметрично
  (знак сохраняется); проверить тестами при pp·ramp/60 ± slip < 0.
- Ветка публикуется на приёмку (ПК-2 как приёмщик по решению пользователя 01.09).

---
*TZ для реализации агентом (web AI / ai2). База: origin/main @ fbce551. Стенд: ПК-3, 60 В, vf=300, 10×, monitor-only.*
