# TZ_VF_START_RELIABILITY — надёжный старт V/f (позиционно-независимый)

## Цель
Устранить ненадёжный старт V/f: при некоторых положениях ротора мотор не трогается
(meas~1 RPM, вал стоит). Зафиксировано на стенде 01.09: позиционно-зависимый провал
старта. Причина — стартовый вектор всегда с theta=0 (слабое поле ~15% boost в
фиксированном направлении); при совпадении оси поля с «мёртвой» позицией ротора
момент ~0, поле слишком слабое, чтобы стронуть вал.

## Условия
- Железо: STM32G474RE + 2× STEVAL-IPM20B, DC-link 60 В, мотор в цепи.
- Сборка стенда: `make clean && make EXTRA_CFLAGS="-DOEW_HS1_COMMISSIONING_RELEASE=1 -DOEW_SD_MONITOR_ONLY=1"`.
- База: `origin/main` (fbce551).
- UART: `@VFLOG`, команды `vf=300`, `vf=0`, `p?`; энкодер AS5048A (`ENC_GetSpeed_rpm()`,
  `ENC_GetAngle14()`), V/f использует его только для скорости (измерение).
- Провал воспроизводится: старт в «мёртвых» положениях ротора → meas~1, V/f молча
  «работает» (fe/theta растут), вал стоит.

## Проблема (баг-репорт, точное место)
`src/vf_control.c:128-162` (VFC_Start), ключевое — строка 139:
```c
vfc.theta_elec = 0;
vfc_make_vector(vfc.theta_elec, vfc.v_boost_pct, &mu, &mv, &mw);
```
При theta=0: mu = mag·sin(0) = 0, mv = +mag·sin(120°), mw = −mag·sin(240°) — поле
статора направлено фиксированно (ось V). v_boost_pct=15% → модуляция слабая
(CCR-сдвиг ~75 тиков от середины). Момент = f(δ), δ — угол между полем статора и
осью ротора; при δ≈0 момент ≈ 0 и слабое поле не преодолевает трение/cogging →
вал стоит. Позиционная зависимость — прямое следствие фиксированного theta=0.

## Требования к решению

### 1. Механизм старта — на выбор агента (один из двух, с обоснованием в отчёте)

**Вариант A (основной, рекомендуется): раскачка theta на старте.**
В VFC_Start (или в первых тиках VFC_Update, пока measured < порога трогания):
- Поле качается: `theta_elec = A·sin(2π·f_swing·t)` вокруг начального угла,
  A = 30–60° эл. (0x15555555–0x2AAAAAAB q31), f_swing = 1–3 Гц, 3–5 периодов
  (T_swing ≤ 2 с) — момент при качании проходит все δ, вал стронется из любого
  положения.
- После T_swing: плавный переход к нормальной рампе (theta продолжает вращаться
  от текущего значения, без скачка фазы).
- vmag на старте = v_boost_pct (не менять характеристику; при желании разрешён
  короткий boost-импульс до 1.5·boost на ≤200 мс — обосновать).

**Вариант B: перебор стартовых углов (fail-closed).**
- Стартовые theta ∈ {0°, 60°, 120°, 180°, 240°, 300°} эл. (6 попыток = полный
  электрический оборот; для pp=6 это 60° мех. шага — покрывает все положения).
- Попытка: VFC_Start с углом, наблюдение до 500 мс: если `|measured_rpm| < 5`
  (вал не тронулся) — VFC_Stop, следующий угол.
- После 6 неудачных попыток: вернуть fail-closed код старта (новый
  `VFC_START_STALLED` в vf_control.h, напр. −9) и НЕ оставлять PWM включённым.
- Углы перебирать детерминированно (не случайно) — воспроизводимость.

Оба варианта: **не менять** VFC_Update-логику рампы/slip после успешного трогания;
стартовая последовательность должна быть частью VFC_Start/ограниченного стартового
окна, а не постоянного режима.

### 2. Защита от бесконечного «молчаливого» старта
Сейчас при застревании V/f работает с meas~1 бесконечно (fe/theta растут, вал стоит).
Добавить в VFC_Update стартовый watchdog: если через T_stall (2 с) после VFC_Start
`|measured| < 5` — VFC_Stop + (для варианта B) переход к следующему углу / возврат
VFC_START_STALLED. fail-closed: PWM выключен, PROTECT не задействован (это не fault,
это отказ старта) — по согласованию: можно логировать через `@VFLOG` поле
`start=stall`.

### 3. Тесты (hosted, tests/vf_control_test.c + при необходимости новые)
- **«start: theta_elec инициализируется ненулевой/раскачкой»** (вариант A): после
  VFC_Start первые тики меняют theta_elec по закону качания (проверить 2-3 тика,
  delta ≠ 0 и знак меняется); после T_swing — нормальное вращение.
- **«start: watchdog останавливает при meas<5 через T_stall»** (оба варианта):
  run_updates с measured=1 на 2·T_stall → VFC_IsRunning()==0, PWM_Disable вызван.
- **«start: перебор углов»** (вариант B): мок ENC_GetSpeed_rpm=1 (не тронулся) →
  после 6 попыток rc == VFC_START_STALLED; при моке «тронулся» на 3-м угле → старт OK.
- Прогнать: `make test` (hosted + QEMU + pytest) — все зелёные.

## Критерии успеха (стенд, 60 В, monitor-only образ)
1. **10/10 стартов** vf=300 при роторе, ВРУЧНУЮ установленном в ≥5 разных положений
   (включая те, где раньше был провал meas~1). На каждом положении: вал трогается
   (meas растёт от 0) в течение первых 2 с, разгон до ≥ ramp/2 к концу окна 4.1 с.
2. Ни одного fault (FAULT_R=18 / STOPPED); при застревании — штатный стоп/перебор
   без зависания PWM.
3. После трогания поведение рампы/slip НЕ изменилось (см. TZ_VF_OVERSHOOT_FIX:
   f_e от рампы, max_meas ≤ 360 RPM).
4. `make test` зелёный; CI зелёный на ветке.

## Deliverables
1. Патч: `src/vf_control.c`, `src/vf_control.h` (код старта + watchdog, при
   варианте B — VFC_START_STALLED), `tests/vf_control_test.c`.
2. Локально: `make && make test` PASS, commissioning PASS.
3. Ветка `ai2/vf-start-reliability` от свежего `origin/main` + CI зелёный.
4. Стендовая кампания: 10/10 стартов в ≥5 положениях ротора (протокол с указанием
   положений и meas-траекторий).

## История контекста
- Провал старта позиционно-зависимый (стенд 01.09, VF_RETURN_PK2_sd_monitor_only.md):
  «при некоторых положениях ротора V/f с theta=0 не трогает вал (meas~1)».
- Стартовая сборка: energize + SD monitor-only (BKE=0) — монитор-режим не влияет на
  меanику старта, но обязателен для V/f-сессий (иначе ложный FAULT_N STEVAL-2 убивает
  разгон — см. пакет sd-monitor).
- Текущий старт: VFC_Start фиксирует theta=0, затем VFC_Update ведёт рампу и slip
  (vf_control.c). vf_start.c (FOC-стартер) НЕ трогать.
- Калибровка полюсов/смещения энкодера НЕ входит в это ТЗ (отдельная задача, если
  понадобится старт по абсолютному углу ротора).

## Примечания
- Safety-модуль vf_control.c — правка ТОЛЬКО по этому ТЗ (регламент §7).
- Один пакет = одно ТЗ: этот пакет НЕ включает фикс overshoot (TZ_VF_OVERSHOOT_FIX.md —
  отдельная ветка; можно делать параллельно, конфликтов по файлам нет — оба трогают
  vf_control.c, поэтому ветки согласовать порядок приёмки: сначала одна, потом rebase
  второй).
- Не менять VfcApertureContract, не менять vf_start.c, не менять protect-путь.
- Ветка публикуется на приёмку (ПК-2 как приёмщик по решению пользователя 01.09).

---
*TZ для реализации агентом (web AI / ai2). База: origin/main @ fbce551. Стенд: ПК-3, 60 В, vf=300, ротор в ≥5 положениях.*
