# ТЗ: TIM8 (slave, Reset mode) работает на 100 кГц вместо 50 кГц

Проект: FOC-двигатель на STM32G474RE, CMSIS-only (без HAL), ARM GCC, make.
Два инвертора: TIM1 (master) + TIM8 (slave, синхронизация через Reset mode).

## Проблема (измерено sigrok 8 МГц)
При ШИМ 50 кГц (ARR=99, TCLK=10 МГц, center-aligned mode 3):
- TIM1 выходы (PC0/PC1/PC2, HIN Inv1): **50.45 кГц, duty 15%** — правильно ✅
- TIM8 выходы (PC10/PC11/PC12, LIN Inv2): **100.9 кГц (ровно ×2!), duty 69.6%** — неверно ❌

Оба таймера настроены одинаково (ARR=99, CCR=15, CMS=11, DTG 1500 нс).
Маска CCER включает комплементарные пары (CCxE|CCxNE). GPIO в AF4 — проверено.

## Конфигурация (src/pwm.c)
- TIM1: CR1 = CMS_1|CMS_0 (center-aligned mode 3), CR2 MMS=010 (Update event → TRGO)
- TIM8: SMCR = SMS=100 (Reset mode), TS=000 (ITR0 = TIM1_TRGOUT) — строка 111
- Оба: ARR=99, CCR=15, BDTR = DTG|OSSR|OSSI|AOE, CEN оба

## Гипотеза (проверить и подтвердить)
По RM0440: в **center-aligned** режиме Update event (UEV) генерируется
**ДВАЖДЫ за период ШИМ** — на переполнении (overflow) и на загрузке (underflow).
Так как TIM8 в Reset mode сбрасывается по каждому TIM1_TRGO (=UEV), он
перезапускается 2 раза за период → считает 0..99 за ПОЛУпериод →
выходная частота TIM8 = 2×50 кГц = 100 кГц. Измерение 100.9 кГц подтверждает.

## Задача
1. Подтвердить гипотезу по RM0440 (раздел Center-aligned mode / Update event,
   RCR Repetition Counter).
2. Предложить фикс: установить **TIM1->RCR = 1** (репетиционный счётчик) —
   UEV будет генерироваться каждые 2 события (т.е. 1 раз за полный период),
   TIM8 будет сбрасываться 1 раз за период → 50 кГц.
   Проверить: RCR на TIM1 (master) или TIM8 (slave)? RCR применяется к UEV
   самого таймера — нужен RCR=1 именно на TIM1, т.к. TRGO = UEV от TIM1.
3. Проверить побочные эффекты:
   - ADC injected group триггерится от TIM1_TRGO (JEXTSEL=0) — при RCR=1
     семплирование станет 1 раз за период вместо 2. Это влияет на FOC-ISR
     (ADC1_2_IRQHandler по JEOS). Оценить, нужно ли менять что-то ещё.
   - Dead-time (DTG) не зависит от RCR — проверить.
   - Синхронный старт TIM1→TIM8 (CEN TIM1 → TRGO → reset TIM8) — не сломается.
4. Дать изменённый код (минимальный: добавить RCR=1 в инициализацию TIM1,
   строка ~95 pwm.c, перед стартом) + команду сборки.

## Ограничения
- Менять ТОЛЬКО репетиционный счётчик (RCR). Не трогать PSC/ARR/CCR/DTG/CCER/CMS.
- Не добавлять магические числа без комментария.
- Проверить, что `TIM_CR1_...`/`TIM_RCR` макросы существуют в stm32g474xx.h.

## Критерий приёмки
- Сборка без ошибок.
- После прошивки: TIM8 выходы (PC10/PC11/PC12) = **50 кГц ±1%**, duty LIN ≈ 85%−dead-time.
- TIM1 выходы остаются 50 кГц / 15%.
- ADC-семплирование FOC не сломано (FOC запускается, JEOS приходит).

## Ответ
Дай: подтверждение/опровержение гипотезы по RM0440, точное место установки RCR
(регистр TIM_RCR, значение 1), изменённые строки кода, оценку влияния на
ADC/FOC/dead-time, команду сборки, ожидаемый результат.
