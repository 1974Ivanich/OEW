# STEP-0 ОТЧЁТ: осуществимость съёма внутри PWM-периода (по коду, стенд не задействован)

Ревизия: `origin/main = 38b66a5a1751354a52b2d1c70eb673b5ed49e76f`; ветка ТЗ `ai2/tz-ls-observability
@ c0111ce6f356accd5a21d5203e17cecf4b17ceae`. Ничего не менялось — только чтение кода.

## 1. Что подтверждено

```text
adc.c:185  static int adc_regular_read(ADC_TypeDef*, channel, uint16_t *out)
   отказывает, если взведён JADSTART ИЛИ ADSTART;
   SQR1 = channel (одиночная регулярная последовательность);
   ISR: сброс EOC/EOS/OVR;  CR |= ADSTART  ← ПРОГРАММНЫЙ запуск;
   ожидание EOC с таймаутом;  *out = DR.
   ⇒ КАЖДЫЙ вызов = одна конверсия одного канала в МОМЕНТ, выбранный вызывающим кодом.

adc.c:521  int ADC_StartConversion(void)
   отказывает при взведённом JADSTART (любой из ADC);
   последовательно читает 4 канала: ADC1/SHUNT1 → ADC2/SHUNT2 → ADC2/CT → ADC2/VBUS;
   публикует кадр со status = ADC_FRAME_SERVICE_BUSY, timestamp_cycles, tim1_sector/sample_window;
   JDR-путь (инжектированный, TRGO) при этом НЕ используется.
   ⇒ поля §2 ТЗ (raw I1/I2/CT/VBUS + ts) доступны БЕЗ правок safety-модулей.

adc.c:581  ADC_ReadInjected() — инжектированный путь (ждёт JEOS на обоих ADC, коммитит кадр) — это путь
   TRGO, которым пользуются probe/ls/map capture; он и создаёт ограничение (см. §3).
```

## 2. Количественные ограничения (важно для анализа)

```text
Звено тайминга: период ШИМ 200 мкс; SMPR=7 = 640.5 такта АЦП; fADC ≈ 48 МГц ⇒ ≈13.3 мкс на канал.
Последовательность ADC_StartConversion() из 4 каналов ⇒ S&H разных каналов разнесены ≈13/27/40 мкс
   от момента вызова. Это НЕ одновременный снимок: строго «моментным» каналом свипа является ПЕРВЫЙ
   в последовательности — raw_idc1 (I1); I2/CT/VBUS идут как контекст со сдвигом.
Разрешение свипа: ≈13 мкс ⇒ ≈15 позиций на период 200 мкс (в ТЗ заявлено ≥8 — выполнимо).
```

## 3. Ограничение, найденное по коду (решение владельца)

```text
Прямой свип момента съёма требует, чтобы во время серии PWM был ВКЛЮЧЁН, а инжектированная группа
НЕ взведена (иначе adc_regular_read() отказывает по JADSTART).
Но единственный разрешённый сегодня энергопуть — PWM_ServiceCaptureStart() — устроен как
«сначала вооружаем инжектированный ADC, затем сервисный запуск PWM» (комментарий в реализации probe/ls,
map_capture.c:179 → :213). ⇒ на разрешённом пути инжектированная группа ВЗВЕДЕНА, и
ADC_StartConversion() вернёт -1.
Обойти это можно только правкой одного из safety-модулей:
  · отдельный сервисный запуск PWM БЕЗ взведения инжектированной группы (pwm.c), или
  · смещение положения инжектированного триггера (MMS/CCR4, pwm.c), или
  · уменьшение SMPR (adc.c) — заведомо лишнее.
Это НЕ входит в ТЗ-LS-OBSERVABILITY и требует явного решения владельца.
```

## 4. Эквивалентный путь, который НЕ требует правок (рекомендуется как основной)

```text
Вместо сдвига МОМЕНТА съёма сдвигается ФАЗА ПЕРЕКЛЮЧЕНИЯ относительно фиксированного момента съёма:
момент съёма на существующем пути задан (граница периода, MMS=2 = UEV), а положение фронта ШИМ
определяется CCR ⇒ меняя вектор/CCR, мы меняем ОТНОСИТЕЛЬНУЮ фазу «фронт ↔ съём», что и проверяет
гипотезу алиасинга, не трогая ни pwm.c, ни adc.c.
  · инструментально это уже есть: профиль сетки (4 вектора на (сектор, окно), pulse_count=8) и
    сервисный вход PWM_ServiceCaptureStart();
  · на каждый вектор — 8 записей; для свипа нужно ~8…15 различных векторов ⇒ 64…120 записей за серию;
  · обязательное дополнение к телеметрии: СНИМОК CCR1..3 обоих таймеров в каждой записи (читается
    напрямую через CMSIS: TIM1->CCR1..3, TIM8->CCR1..3) — без него относительная фаза не восстанавливается;
  · интерпретация: PWM-ALIASED ⇔ отклик появляется/исчезает в зависимости от вектора при неизменной
    геометрии измерения; NOT_OBSERVABLE ⇔ отклик отсутствует при всех проверенных относительных фазах.
Честная граница этого варианта: он проверяет относительную фазу, но НЕ проверяет разные моменты съёма
при одной и той же форме ШИМ; для строгого разделения H1/H2 он слабее прямого свипа момента.
```

## 5. Что это меняет в ТЗ (предлагаемая правка §4, отдельной веткой)

```text
· §4: зафиксировать две реализации — (A) прямой свип момента съёма (требует решения владельца по pwm.c)
  и (B) эквивалентный свип относительной фазы на существующем пути (реализуем без правок);
· §2: добавить обязательные поля raw-снимков TIM1->CCR1..3 и TIM8->CCR1..3;
· §2: прямо указать, что «моментным» каналом является raw_idc1, а I2/CT/VBUS — контекст со сдвигом ~13/27/40 мкс;
· §5: для варианта (B) критерий OBSERVABLE/NOT_OBSERVABLE проверяется по зависимости от вектора, а
  формулировка PWM-ALIASED ограничивается «зависимостью от относительной фазы» (не от момента съёма).
```
