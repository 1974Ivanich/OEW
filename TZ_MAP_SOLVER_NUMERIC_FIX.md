# ТЗ: Численный фикс MapMeasurement_SolveM — работа в mA без потери точности (без переполнения int64)

**Проект:** OEW Motor Drive — STM32G474RE, CMSIS-only, OEW dual-inverter FOC (2× STEVAL-IPM20B), асинхронный двигатель.
**База:** HEAD 7a7a7c4 (default-deny + SD-direct + P0 clear-race).
**Цель:** устранить усечение токов до целых ампер в `src/map_measurement_solver.c` (ложный `MAP_SOLVER_SINGULAR` при токах < 1 А, деградация conditioning при 1–2 А) при сохранении защиты от переполнения `int64`, которой сейчас (грубо) служит деление на 1000.

**Модуль НЕ подключён к рантайму** (вызовы только из `tests/map_solver_certifier_test.c:67,91,92`) — фикс безопасен для прошивки, но обязан сохранить PASS всех существующих тестов.

---

## 1. Дефект (подтверждён аудитом и расчётом)

`src/map_measurement_solver.c:44-47`:

```c
static int32_t scaled_ma(int32_t value)
{
    return value / 1000;
}
```

Применяется к `idc1_ma`/`idc2_ma` и фазам (строки 101-106, 134-137) ДО накопления `s00/s01/s11/t00…`. Следствия:

1. При токах **< 1000 мА** обе оси дают 0 → `s00=s01=s11=0` → `determinant=0` → `MAP_SOLVER_SINGULAR` (строки 115-118) — ложный отказ на годных данных (типовой диапазон щадящей commissioning-съёмки).
2. При 1–2 А — квантование шагом 1000 мА резко ухудшает conditioning и residual/bias (`MAP_SOLVER_RESIDUAL_BAD`/`HOLDOUT_BAD` на годных данных, либо маскировка реальной ошибки полярности/gain).
3. Нарушает спецификацию: `docs/MAP_ACCUMULATOR_SPEC.md:119` — «Значения должны оставаться в инженерных единицах mA до вычисления статистики. Нельзя округлять каждый sample…».

**⚠️ Прямое «убрать scaled_ma» НЕДОПУСТИМО** — переполнение `int64` (проверено расчётом):

| Выражение | Значение при 12 А, N=32 (мА-версия) | int64 max |
|---|---|---|
| `s00 = Σ x0²` | 4.6×10⁹ | 9.2×10¹⁸ ✅ |
| `det = s00·s11 − s01²` (ортогональные каналы) | **2.12×10¹⁹** | ❌ |
| `t00·s11` (числитель m00) | ≤ 2.1×10¹⁹ | ❌ |
| `trace²` (condition ratio, строка 120) | ≤ 8.5×10¹⁹ | ❌ |

Порог переполнения det ≈ **9.7 А** (оба канала ортогонально). Существующий юнит-тест использует токи до **20000 мА** → в мА-версии `det ≤ 1.64×10²⁰` — переполнение гарантировано.

---

## 2. Требования к фиксу (`src/map_measurement_solver.c`)

### 2.1 Убрать `scaled_ma` (строки 44-47) полностью
`x0/x1/y0/y1` берутся в исходных мА (строки 101-106, 134-137). `s00/s01/s11/t00/t01/t10/t11` накапливаются в `int64` — диапазон безопасен (≤ 4.6×10⁹).

### 2.2 Защита от переполнения — `__int128` для промежуточных произведений
GCC (arm-none-eabi 13.2 и hosted-GCC тестов) поддерживает `__int128`. Использовать его **только** там, где произведение двух int64 может переполниться:

- `determinant = (__int128)s00 * s11 - (__int128)s01 * s01;` (строка 115)
- Числители `m00/m01/m10/m11` (строки 123-126): `((__int128)t00 * s11 - (__int128)t01 * s01) * CURRENT_RECON_COEFF_SCALE / determinant`
- `trace` и condition ratio (строки 119-120): `trace` оставить в `int64` (≤ 9.2×10⁹ безопасно), но `(__int128)trace * trace / determinant` для сравнения с `max_condition_ratio`
- Сравнение `determinant < qualification->min_abs_determinant` — с НОРМИРОВАННЫМ det (см. 2.3)

**Запрещено:** `double`/`float` в вычислительном ядре (модуль — pure integer, CMSIS-стиль проекта), размазывание `__int128` на весь файл, изменение формул МНК.

### 2.3 Сохранить единицы порогов qualification (обратная совместимость)
`MapSolverQualification.min_abs_determinant` (и `report->determinant_scaled`) сейчас калиброваны под det в единицах «А⁴» (тест: `min_abs_determinant = 1`). В мА-версии det вырастает в 10¹² раз. **Не менять** семантику констант в `map_measurement_solver.h` и тестах:

- Ввести `det_norm = determinant / 1000000000000LL` (10¹²) в `int64`; сравнение с `min_abs_determinant` (строка 116) и `report->determinant_scaled` (строка 167) — по `det_norm`
- **Condition ratio (строка 120) и `report->condition_ratio` (строка 168) — НЕ нормировать:** `trace²/det` безразмерно и масштаб-инвариантно, но вычислять через `__int128` (см. 2.2) для защиты от переполнения `trace²`
- Порядок операций: сначала `__int128`-произведения, потом деление на 10¹², результат в `int64` — чтобы не потерять точность малых det

### 2.4 Residual — убрать компенсирующее масштабирование
Строки 138-139:

```c
int32_t e0 = (predict_scaled(m00, m01, x0, x1) - y0) * 1000;   // было: y в А, *1000 → мА
int32_t e1 = (predict_scaled(m10, m11, x0, x1) - y1) * 1000;
```

В мА-версии `y0/y1` уже в мА, `predict_scaled` возвращает мА (размерность: `m` — безразмерный ×SCALE, `x` — мА, деление на `CURRENT_RECON_COEFF_SCALE=1000` → мА). Стало:

```c
int32_t e0 = predict_scaled(m00, m01, x0, x1) - y0;
int32_t e1 = predict_scaled(m10, m11, x0, x1) - y1;
```

`predict_scaled` (строки 49-54), `clamp_i32`, `abs32s`, `isqrt_u64`, KCL (строка 141-144), отчёты `*_rms_ma`/`*_max_ma`/`*_bias_ma` (строки 156-160) — **без изменений** (единицы мА уже корректны после 2.4).

### 2.5 Запрещённые изменения
- `src/map_builder.c`, `src/current_map_selector.c`, `src/map_measurement_solver.h` (структуры, enum, сигнатуры) — **не трогать**
- `MapMeasurement_SolveM` сигнатура и контракт возвратов (`MAP_SOLVER_*`) — без изменений
- Математика Cramer's rule (строки 123-126) — без изменений (корректна)
- Другие модули/тесты — только `tests/map_solver_certifier_test.c`

---

## 3. Тесты (`tests/map_solver_certifier_test.c`)

Добавить **три новых сценария** (существующие — оставить, должны остаться PASS):

1. **Низкоамперная регрессия (главная):** 8-12 сэмплов, токи **200–900 мА** (обе оси, некоррелированные, напр. `x0 = 200+i·100`, `x1 = 800−i·100`), фазы = известная линейная модель `phase_a = 2·x0 + x1` (как в существующем тесте, :33-36). Ожидание: `MAP_SOLVER_OK` (раньше — ложный `SINGULAR`), `report->residual_rms_ma` мал (< 50 мА), оценка `m00/m01` близка к истинной (допуск ±10 %), `report->determinant_scaled > 0`.
2. **Переполнение (регрессия на 2.2):** токи **12–20 А** с ортогональными каналами (напр. `x0 = 15000` всегда, `x1 = ±15000` пополам) — случай, где наивная мА-версия переполняет det. Ожидание: `MAP_SOLVER_OK` с корректными m (нет UB/отрицательного det).
3. **Граничный det (проверка 2.3):** сэмплы с почти вырожденной матрицей (`x1 = x0·k`, k≈1, токи ~5 А) — ожидание: `MAP_SOLVER_CONDITION_BAD` или `SINGULAR` по нормированному det (тот же вердикт, что и в старой А⁴-версии при тех же данных) — подтверждает сохранение семантики порогов.

Все сценарии — в рамках существующего фреймворка теста (assert'ы + print PASS/FAIL), hosted + QEMU.

---

## 4. Проверки (обязательные, на рабочем дереве базы 7a7a7c4)

```bash
make                 # production build — PASS
make test            # hosted — все тесты PASS (включая 3 новых сценария)
make test-qemu       # QEMU — PASS
```

В отчёте указать: файл:строка каждого изменения, таблицу «было/стало» для 2.1-2.4, результаты прогонов.

---

## 5. Формат сдачи

Как обычно: единый unified-diff патч к HEAD 7a7a7c4 + краткое описание. Патч применяется к рабочему дереву (наши правки уже интегрированы) — при невозможности применить `git apply` (смещения контекста) прикладывать дифф с указанием точных файлов, интеграцию выполнит агент вручную.
