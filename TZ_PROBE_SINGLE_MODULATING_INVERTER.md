# ТЗ: probe rev7 — ОДИН модулирующий инвертор, чтение шунта немодулирующего

Проект: STM32G474RE, CMSIS-only, OEW (2×STEVAL-IPM20B).
Ветка-исполнитель: `ai2/tz2-probe-single-modulating` от `origin/main`. Один пакет = одно ТЗ.
Статус: **HOLD** — исполнять после физической проверки PA6/CT (пакет `ct_pa6_check_20260919`).

## 1. Проблема (почему rev4…rev6 не могли дать фазную конвенцию)

`pinout.md` («Канал-правило», раздел 3 «Токи и напряжение»):

```text
I1 = PA0 = выход ОУ инвертора №1 с его ОБЩЕГО DC-link шунта;  I2 = PA1 — аналогично для №2.
I1/I2 — НЕ фазные токи: каждый видит СУММУ фаз только в НУЛЕВОМ ВЕКТОРЕ своего инвертора.
Канал-правило: для СТАТИЧЕСКИХ тестов использовать шунт НЕ МОДУЛИРУЮЩЕГО инвертора.
Ires = PA6 = выход трансформаторного CT (три провода фаз инвертора №1 продеты);
       канал HW-0 (без bias и ОУ, законный нуль raw ≈ 0), знак не квалифицирован.
```

В `Autotune_ProbePhase` (rev4…rev6) возбуждались **оба** инвертора одинаково (общая мода:
`TIM1->CCRn = TIM8->CCRn = half ± ccr`). Значит оба шунта модулирующие, и читаемая величина
задаётся модуляцией, а не током обмотки. Этим объясняется наблюдённая в rev6
фазо-неспецифичность отклика (U/V/W дали один и тот же знак и почти одну величину).
Тот же дефект топологии у теста `ls` (дифференциальный привод `half ± ccr` — тоже оба
инвертора модулирующие).

## 2. Цель

Получить данные, в которых шунт **немодулирующего** инвертора несёт информацию о фактическом
фазном токе, и по ним (а не по модуляции) проверять фазную конвенцию и знак.
`Ires` продолжает логироваться, но **не** используется как обязательный источник конвенции
(HW-0 / ZSV-диагностика; `raw_ct ≈ 0` — ожидаемое поведение).

Конфигурация:

```text
            INV1 (TIM1)                 INV2 (TIM8)
                 │                           │
              MODULATE                   ZERO VECTOR
        одна фаза X: half ± ccr        все три фазы: half
                 │                           │
          I1 = модулирующий шунт      I2 = НЕ модулирующий шунт  ← целевой сигнал
```

## 3. Контракт (телеметрия)

Сохраняется всё из rev6 (`ZERO_PRE/TEST/ZERO_POST`, сырые коды, `jeos`, `DRIFT`, `DELTA`,
`SIGN`, `CCR`, `OK`) и добавляется симметричная пара ±5 % и признак `odd`:

```
@DBG:CHx:ARM:arr=..:d_pct=5:ccr_hi=..:ccr_lo=..:vbus_mv=..:off_i1=..:off_i2=..:off_ct=..
            :sec=..:win=..:rev=..:inv_mod=1:inv_static=2:ccr_mod=..:ccr_static=..
            :pattern_tim1=..,..,..:pattern_tim8=..,..,..
@DBG:CHx:ZERO_PRE :raw_i1 raw_i2 raw_ct :i1 i2 ires :vbus :jeos      (нейтраль, спад 20 мс)
@DBG:CHx:EXC_POS  :tim1=..,..,..:tim8=..,..,..                       (+5 % на фазе X, Inv1)
@DBG:CHx:CCR_POS  :tim1=..,..,..:tim8=..,..,..   ← ЖИВОЙ снимок РЕГИСТРОВ сразу после EXC_POS
@DBG:CHx:TEST_POS :raw_i1 raw_i2 raw_ct :i1 i2 ires :vbus :jeos
@DBG:CHx:ZERO_MID :...                                               (возбуждение снято, спад 20 мс)
@DBG:CHx:EXC_NEG  :tim1=..,..,..:tim8=..,..,..                       (−5 % на той же фазе X)
@DBG:CHx:CCR_NEG  :tim1=..,..,..:tim8=..,..,..   ← ЖИВОЙ снимок РЕГИСТРОВ сразу после EXC_NEG
@DBG:CHx:TEST_NEG :raw_i1 raw_i2 raw_ct :i1 i2 ires :vbus :jeos
@DBG:CHx:ZERO_POST:...                                               (возбуждение снято, спад 20 мс)
@DBG:CHx:DELTA    :d_i1=..:d_i2=..:d_ires=..        (TEST_POS − ZERO_PRE, как в rev6)
@DBG:CHx:SIGN     :s_i1=..:s_i2=..:s_ires=..:dom=..:dom_delta=..
@DBG:CHx:ODD      :d_plus_i1=..:d_plus_i2=..:d_minus_i1=..:d_minus_i2=..
                  :raw_d_plus_i1=..:raw_d_plus_i2=..:raw_d_minus_i1=..:raw_d_minus_i2=..
                  :prod_i1=..:prod_i2=..:ratio_i1=..:ratio_i2=..
@DBG:CHx:DRIFT    :ZERO_MID−ZERO_PRE и ZERO_POST−ZERO_PRE (мА и raw): dpre_*:dpost_*:raw_*
@DBG:CHx:CCR / OK
```

Семантика новых полей:

- `inv_mod` = 1 (модулирует TIM1/инвертор №1), `inv_static` = 2 (в нулевом векторе);
- `ccr_mod` — CCR возбуждённой фазы, `ccr_static` = `half` (все три фазы немодулирующего);
- `CCR_POS` / `CCR_NEG` — **живой снимок регистров TIM1/TIM8 сразу после каждой ступени**:
  обязательное доказательство фактического состояния (ARM лишь объявляет конфигурацию);
- `d_plus_*` = `TEST_POS − ZERO_PRE`;  **`d_minus_*` = `TEST_NEG − ZERO_MID`** —
  база для отрицательной ступени именно `ZERO_MID`, иначе дрейф `ZERO_PRE→ZERO_MID`
  смешается с `D−`. То же правило для raw-дельт (`raw_d_plus_*`, `raw_d_minus_*`);
- `dpre_*` = `ZERO_MID − ZERO_PRE`, `dpost_*` = `ZERO_POST − ZERO_PRE` — дрейфы базы,
  отдельно от откликов;
- `prod_i*` = знак `d_plus·d_minus` (−1 / 0 / +1) — **признак нечётности (odd-response),
  НЕ доказательство того, что измеряется фазный ток**; `ratio_i*` = `|d_minus|/|d_plus|` ×100;
- **ожидаемый знак `I2` для U/V/W заранее НЕ задаётся** — эксперимент его измеряет.

## 4. Что именно делать (правка)

Только `Autotune_ProbePhase()` в `src/autotune.c` (+ при необходимости один статический хелпер):

1. Сервéрный вход — как в rev6: нейтральный паттерн для арма, `ADC_InjectedStart()` →
   `PWM_ServiceCaptureStart(pattern)`; предусловия, VBUS ≥ 24 В, состояния — без изменений.
2. Возбуждение: **только Inv1** — `TIM1->CCR[n_phase] = half ± ccr`; остальные CCR TIM1 = `half`;
   **все три CCR TIM8 = `half`** (нулевой вектор, инвертор №2 не модулирует).
   `TIM1->CCR*` правятся прямой записью + `TIM1->EGR |= TIM_EGR_UG` (как в rev6).
3. Порядок: ZERO_PRE (спад 20 мс) → EXC_POS/TEST_POS → снять возбуждение, спад 20 мс → ZERO_MID
   → EXC_NEG/TEST_NEG → снять, спад 20 мс → ZERO_POST → DELTA/SIGN/ODD/DRIFT/CCR/OK.
4. Чтение каналов — только `ADC_ReadInjected()` (см. дефект rev4), медиана 5 кадров, raw+мА.
5. `g_autotune_abort` проверяется на каждом шаге; стоп на любом выходе —
   `PWM_Disable(); PWM_TriggerLow(); ADC_InjectedStop(); AT_TestEnd(&session);`
6. Режим 5 % и уровень — не меняются (симметричная пара ±5 % не расширяет силовой эксперимент:
   тот же модуль, только два знака).

## 5. Объём изменений

```
src/autotune.c        только Autotune_ProbePhase() (+ один хелпер, если нужен)
больше ничего: both_enable(), foc.c, pwm.c, adc.c, protect.c, adc_dispatch.c,
               main.c, Makefile, *.ioc, tests/*.c — НЕ трогать
```

## 6. Конверт и безопасность

- VBUS 24…36 В; звено подаётся по обычной процедуре (DC link OFF → LOTO → ≥ 60 с → DMM < 1 В).
- Возбуждение: одна фаза, ±5 % от нейтрали (при `arr=999` — ±49 отсчётов), ≤ 20 мс на ступень,
  два TEST-состояния на команду.
- Ожидаемые токи — сотни мА (ограничены сопротивлением обмотки); аппаратный предел 2 А,
  BKIN/EM_STOP, `AT_SafetyCheck` — без изменений.
- Новых путей включения нет: используется тот же сервисный вход, что в `ls`/rev6.

## 7. Проверки

1. `make` — чисто; `make test` — PASS; `scripts/cubemx_check.py` — PASS (периферия не менялась).
2. Дифф: изменён только `src/autotune.c`; `both_enable()` побайтово идентична `origin/main`.
3. CI build-test — success.
4. Стенд: `chu`, `chv`, `chw` по 3 повтора, VBUS 24…36 В, полный непрерывный лог;
   после каждой команды `p?` + `breakdiag` (CCER=0 · MOE=0 · @BRK:valid=0).
5. Приёмка данных: `probe_matrix` v3.1 (двухуровневые гейты) + независимая проверка сырых
   PRE/TEST/POST перед сопоставлением с вердиктом инструмента.

## 8. Что докажет и что не докажет

Докажет: несёт ли шунт немодулирующего инвертора сигнал, связанный с фактическим фазным током;
появляется ли фазовая специфичность (различие U/V/W); является ли отклик нечётным
(`prod < 0`) и линейным (`|D+| ≈ |D−|`). Знак `I2` для U/V/W заранее НЕ предполагается —
он измеряется. `prod < 0` — признак нечётности, а НЕ доказательство измерения фазного тока.
**Не докажет**: пригодность FOC current reconstruction, корректность карты OEW, номинал Rs,
и не является измерением `Ls`.

## 9. Запреты

- Не менять уровень 5 %, схему силового маршрута, dead-time, паттерн за пределы описанного.
- Не трогать `both_enable()` и другие энергии.тесты (`oew`, `lspos`, `ch`, `irot`, `inertia`, `rr`).
- Не использовать `Ires` как обязательный источник фазной конвенции (HW-0).
- Не подставлять значения для `mp=` и не делать выводов о `Ls`.

## 10. Критерии приёмки (три уровня, порядок обязателен)

### 10.1 HARD (блокируют; по ФАКТИЧЕСКОМУ логу, не по коду)

```text
acquisition живой:            jeos(ZERO_PRE) < jeos(TEST_POS) < jeos(ZERO_MID) <
                              jeos(TEST_NEG) < jeos(ZERO_POST)
POS и NEG реально выполнены:  в каждом блоке есть EXC_POS/CCR_POS/TEST_POS и EXC_NEG/CCR_NEG/TEST_NEG
INV1 действительно модулирует:
                              CCR_POS: TIM1 CCR[фаза X] = ccr_hi, два других = half
                              CCR_NEG: TIM1 CCR[фаза X] = ccr_lo, два других = half
INV2 действительно в нулевом векторе:
                              в ОБОИХ снимках TIM8 CCR1/CCR2/CCR3 = 500/500/500 (= half)
                              (ARM-строка это лишь объявляет; доказательство — снимок)
safety после команды чистый:  CCER=0 · MOE=0 · @BRK:valid=0
UART clean:                   uart_drp=0 · uart_trunc=0 во всех @SYS
все три фазы и 3 повтора:     без пропущенных блоков
```

При нарушении любого пункта — `NOT_QUALIFIED`, данные не «дотягиваются» аналитически.

### 10.2 DIAGNOSTIC (не блокируют переход к §10.3, печатаются с числами)

```text
baseline repeatability:       разброс ZERO_PRE по повторам фазы
дрейф ZERO_PRE → ZERO_MID:    dpre_*
дрейф ZERO_MID → ZERO_POST:   dpost_*
|D+| / |D−|:                  ratio_i1, ratio_i2
prod sign:                    prod_i1, prod_i2 (нечётность, НЕ доказательство фазного тока)
масштаб:                      |D| в LSB (LSB шунта ≈12.7 мА) — справка, не гейт
```

### 10.3 INTERPRETATION (только после всех трёх фаз)

Обсуждать channel convention можно только имея полный набор `U/V/W × I1/I2` в сырых кодах
и мА, по всем повторам. До этого — никаких выводов о конвенции и знаках.

### 10.4 Процедурные требования

1. `make`/`make test`/`cubemx_check`/CI — PASS; дифф строго в разрешённых файлах.
2. ARM-строка содержит `inv_mod=1`, `inv_static=2`, `ccr_static=500` (объявление конфигурации;
   фактическое состояние доказывается снимками `CCR_POS`/`CCR_NEG`).
3. Данные позволяют ответить на вопрос §8 (или честно фиксируют, что не позволяют).
