# ТЗ: Koda — проверка P1/P2 пунктов рецензии foc.c

## Контекст
Проект C:\ST\boyler\Motor — STM32G474 (CMSIS-only), OEW FOC для асинхронного двигателя.
Внешний рецензент дал 22 пункта по src/foc.c. P0 (критические) проверяет другой агент.
ТВОЯ ЗАДАЧА — проверить пункты P1/P2 по коду и дать вердикт: ПОДТВЕРЖДЕНО / ОПРОВЕРГНУТО / ЧАСТИЧНО + доказательство (строки кода).

## Пункты для проверки

**P1:**
- **п.6**: `FOC_MAX_SLIP_HZ = 5` — фиксированный предел. Вопрос: Tr=20мс при Iq/Id=0.5 даёт slip≈4 Гц, а Tr=100мс — 0.8 Гц. Ограничение должно зависеть от параметров? Проверь формулу slip в foc.c (FOC_SLIP_2PI_INV) и реши, корректно ли ±5 Гц как абсолютный предел.
- **п.7** (частично, уже известен fallback 100мс на строке 609): проверь, где ещё используется Tr, и подтверди, что silent fallback 100мс опасен для closed-loop FOC. Есть ли motor_params_valid?
- **п.12**: `VM_Update(&vm, vd, vq)` + `PI_BackCalculation(&pi_d, vm.vd_err)`. Проверь в src/voltage_manager.c: vd_err = limited - commanded или commanded - limited? Знак важен для anti-windup. Посмотри код VM_Update.
- **п.13**: компенсация потерь инвертора: `vd += vcomp_dq.d` затем observer получает `rvu - vcomp_u`. Проверь знаки согласованы.
- **п.14**: dead-time формула `vdt_mag = 2*dt_ns*32768/(Ts_us*1000)` для OEW-топологии (2 инвертора). Проверь в foc.c + pwm.c: учитывает ли коэффициент 2 оба моста правильно (дифференциальная ошибка)?

**P2:**
- **п.4**: `f_slip_hz = slip_dt / FOC_PHASE_PER_HZ` — целочисленное деление, телеметрия вводит в заблуждение при slip < 1 Гц. Подтверди, что управление использует slip_dt (не f_slip_hz) — т.е. это чисто телеметрийная проблема.
- **п.8**: `enc_phase_accum` — uint32 wrap-around = 2π. Терминология «q31» вводит в заблуждение? Проверь комментарии и CORDIC_SinCos — интерпретирует ли он угол как uint32 (2^32=2π)?
- **п.9**: `FOC_OMEGA_PER_ERPM = 14317` vs `FOC_PHASE_PER_HZ = 858993` — точность. 2^32/(60*5000) = 14316.56; 2^32/5000 = 858993.46. Согласованы ли? Есть ли расхождение rpm→phase vs Hz→phase?
- **п.11**: speed PI: `omega_ref = speed_ref_rpm * p * FOC_OMEGA_PER_ERPM; spd_err = (omega_ref - omega_enc) >> 8; PI_Update` с kp=2000, ki=50. Проверь масштабирование: корректен ли >>8, адекватен ли kp=2000 (p_term = kp*error>>15)?
- **п.16**: InvClarke: `sqrt3_vb = (vbeta * FOC_SQRT3_Q15) >> 15` — переполнение int32 при vbeta→INT32_MAX? Реально ли достижимо в Q15-диапазоне (|vbeta|≤32768)?
- **п.17**: `mod_u = CLAMP((vu * 98) / 100, ...)` — безопасно ли в Q15?
- **п.18**: `FOC_SetSpeed` — `__disable_irq()` вокруг VF_SetTarget. Короткая ли секция? Есть ли альтернатива (atomic write)?
- **п.20**: `FOC_DEFAULT_ID_REF_MA = 2000`, `id_target = id_ref_ma / 100` → Id=20 internal = 2A при масштабе 100мА/unit. Согласовано ли с FOC_STARTUP_ID?
- **п.21**: направление slip: `slip_dt ∝ prev_dq_q / prev_dq_d`. Проверь знак Park (`d = alpha*c + beta*s; q = -alpha*s + beta*c`) — даёт ли Iq>0 → положительный slip при стандартной фазировке? Согласовано ли с VF_GetTheta и направлением encoder?

## Формат ответа
По каждому пункту: **ПОДТВЕРЖДЕНО / ОПРОВЕРГНУТО / ЧАСТИЧНО** + 1-3 строки доказательства (номера строк, значения).
В конце: список РЕАЛЬНЫХ проблем с предложением фикса. НЕ менять код — только анализ!

## Правила
- Файлы: src/foc.c, src/voltage_manager.c, src/pwm.c, src/cordic_math.c, src/vf_start.c (если нужен), src/encoder.c
- Точные формулировки: ПОДТВЕРЖДЕНО только при наличии строки кода, ОПРОВЕРГНУТО — если код показывает обратное.
